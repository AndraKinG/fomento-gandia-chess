# Estado y pendientes (actualizar al avanzar)

- Fases 0 (cimientos), 1A (diseño), 1B (equipos/calendario/disponibilidad) y 1C (validador/convocatorias/resultados) **completas y aprobadas** (última revisión global: "Ready to deploy", 166/166 tests).
- Pendiente de usuario al cierre de 1C: **SMTP Resend** (guía en docs/referencia). La prueba de convocatoria en móvil se hizo (jornada A/R99 "Amistoso de prueba") y la limpieza de datos de prueba está hecha (migración 0008, 2026-08-05): borradas las 2 cuentas `*.prueba@fomentogandia.test` con sus profiles/link_requests, las 3 fichas "Jugador Prueba *" y el encuentro R99 con su convocatoria y resultados. Queda 1 sola cuenta (jony9vcf@gmail.com, admin + capitán A). Las 4 fichas que sobrevivían de probar los importadores de ELO (Carlsen, Nakamura, Aalbersberg, Aalders) y un torneo de prueba creado a mano se borraron el 2026-08-06 (migración 0016): `players` y el orden de fuerza contienen ya exactamente los mismos 46 socios.
- **Fase 4 (torneos internos y ELO de club) COMPLETA (2026-08-06)**: migración 0015. Sección **Club** (`/club/interno`): torneos por liguilla o suizo, inscripción con ELO de partida a 1000 para todos (hasta el 2026-08-11 se sacaba del oficial), generación de rondas, resultados, clasificación con Buchholz y ranking de ELO propio. Lógica pura en `src/lib/club/` (elo, emparejar, clasificacion) con 80 tests. **El ELO no se guarda**: se recalcula desde todas las partidas, para que corregir un resultado antiguo no deje el ranking mal para siempre. **Un descanso puntúa 0.5** (regla del club). Los colores de la liguilla se reparten orientando un circuito euleriano: desequilibrio 0 con jugadores impares y 1 con pares, que es el mínimo posible — un reparto codicioso deja a alguien sin blancas nunca.
- **Fase 3 (partidas) COMPLETA (2026-08-05/06)**: migración 0014. Repositorio compartido (`/club/partidas`): subir con datos, **tablero propio para meter las jugadas** (dependencia `chess.js` para las reglas), reproducir PGN guardados, buscar por nombre, importar de Lichess/Chess.com y exportar `.pgn`. Componentes en `src/components/ajedrez/`. Las partidas de torneos internos se enlazan por `club_pairings.game_id`.
- **Fase 2 (torneos y coches) COMPLETA (2026-08-05)**: migración 0010 aplicada, 147 torneos del calendario FACV importados (ninguno marcado de interés todavía), pantallas `/club/torneos`, `/club/torneos/[id]` y `/club/admin/torneos`, y tarjetas de próximos torneos en la home. 257 tests. 7 de los 8 criterios de aceptación verificados en vivo con JWT de socio; **el único pendiente es mirar las pantallas en un móvil de verdad**, que requiere sesión.
- **Onboarding del club (2026-08-05) — COMPLETO Y VERIFICADO (2026-08-07)**: código de acceso activo (rotado el 2026-09-05; se consulta en `/club/admin/acceso` — no se escribe en ningún archivo, el repo es público), migración 0009 aplicada, "Allow new users to sign up" **desactivado en Supabase** y ambos criterios verificados en vivo: POST directo a `/auth/v1/signup` con clave anónima → 422 `signup_disabled`; cuenta temporal sin vincular → 0 filas en las 9 tablas del club (0009 funciona). Solo queda 1 cuenta en la BD: jony9vcf@gmail.com.
- **Sesión 2026-08-07 (PC del trabajo)** — hecho: (a) `git pull` de los 43 commits + BD verificada al día (0001→0021, la 0017 se aplicó ese día); (b) CLAUDE.md reparado de acentos (venía doble-codificado del 06) y regla nueva de migraciones-por-chat escrita; (c) **onboarding verificado en vivo** (signup 422 + lectura cerrada, ver punto de arriba); (d) **sede pública publicada**: portada y `/unirse` muestran el Poliesportiu Municipal (Av. dels Esports 17, Gandia) con enlace a Maps — dato del listado oficial de sedes FACV (`listado_sedes.php?id=1428`), verificado como visible sin sesión. Contacto candidato encontrado pero NO publicado (el teléfono y el correo están en el canal privado, no aquí: son de un socio, el nº 17 — el propietario espera confirmación del club sobre qué publicar).
- **Selector de temporada en Interclubs — HECHO (2026-08-10; la infraestructura y las pantallas desde el PC del trabajo, el cierre y la verificación desde casa).**
  `src/lib/temporadas.ts` (con tests): `elegirTemporada` (la pedida por URL, con red hacia la activa), `conTemporada` (añade `?temporada=` SOLO si no es la activa: los enlaces de siempre no cambian) y `leerTemporadas` (ordenadas por `created_at`, no por nombre). Desplegable en `src/components/ui/SelectorTemporada.tsx` — **no usa `useSearchParams()`**: obligaba a renderizar en cliente hasta el `Suspense` más cercano y el `select` se quedaba sin hidratar en producción (se veía pero no hacía nada).
  Con selector: `/club/equipos` y `/club/orden-fuerza`. `/club/equipos/[id]` NO elige: **el equipo ya lleva su temporada** (`season_id`) y de ahí saca la etiqueta y el volver; la ficha de jornada tampoco (carga por id y vuelve al equipo). **Siguen atadas a la activa A PROPÓSITO**: admin, importadores, `/club/vincular`, el ELO del perfil y de la ficha de socio, y disponibilidad — se importa y se juega sobre la temporada en curso.
  **El desplegable no se pinta con una sola temporada** (una opción es ruido): por eso nadie lo había visto. Verificado en vivo el 2026-08-10 con una temporada `PRUEBA — Temporada vieja` (inactiva y vacía) — **borrarla cuando el propietario dé el visto bueno**.
  **Cómo arranca una temporada**, que hace falta para no romperlo: (1) cambiar `TEMPORADA_ID_FACV` en `src/lib/import/facv-config.ts` — la FACV da un id nuevo a su calendario cada año, es cambio de código y despliegue; (2) crear la temporada pegando su orden de fuerza en la importación manual de Admin → Orden de fuerza, que desactiva la anterior y activa la nueva con rollback si falla; (3) dar de alta los equipos; (4) importar calendario y luego resultados y actas. **Ningún paso es automático.**
  Lo que NO depende de la temporada y por tanto no se pierde de vista: fichas de socios, repositorio de partidas y torneos internos con su ELO de club.
- **Pendiente de diseño**: el repaso en **móvil** de todas las pantallas (el propietario decidió el 2026-08-06 hacer primero escritorio). Y **disponibilidad y la convocatoria del capitán se rediseñaron a ciegas en su estado CON datos**, porque la temporada 2026 ya terminó y no hay jornadas próximas: hay que volver a mirarlas cuando arranque la 2027.
- **Las cuatro fases están hechas.** Lo que queda, por orden de lo que bloquea antes:
  1. **Datos de la web pública** — dirección **HECHA (2026-08-07)**; faltan **horarios y contacto**, que el propietario está esperando que se los confirmen los del club. Cuando lleguen: rellenar en `src/app/page.tsx` y `src/app/unirse/page.tsx` (secciones "Dónde jugamos"/"Dónde estamos", el hueco está preparado y comentado).
  2. ~~Iconos PNG de la PWA~~ **HECHO (2026-08-06)**: SUSTITUIDO el 2026-08-11 por el LOGO OFICIAL del club (el Puente del Fomento hecho de piezas, `public/logo-club.jpg`): la versión "completo" del componente Escudo enseña el mural entero (rectangular, proporción 1128/712), y `scripts/generar-iconos.mjs` genera `marca.png` y todos los iconos de la PWA desde el RECORTE DEL CABALLO del mural (encuadre en el script) en disco con el aro marino — un mural con texto es ilegible a tamaño de icono. `escudo.png` ya no existe. OJO: el fichero del mural vive en `/logo-club.jpg`, NUNCA bajo `/club/...`, que el proxy trata como zona de socios (pasó: el optimizador de imágenes recibía el login). El favicon del andamiaje era el triángulo de Vercel.
  3. **ELO de FEDA — importador ARREGLADO, fuente MUERTA, decisión: no aplicar (2026-08-07).** Cruzaba por `feda_id` y **ninguna de las 46 fichas lo tiene** (todas tienen `fide_id`, que es lo que publica el orden de fuerza de la FACV), así que devolvía siempre "0 actualizados" sin error — de ahí que el perfil enseñara "Fuerza 1400" para todo el club. Ahora cruza por FIDE usando la columna `Id. Fide` de la lista y rellena el `feda_id` que falta. **Pero feda.org no publica listas desde diciembre de 2023** (verificado en su página, y no apuntan a otro origen): lo único importable son datos de hace dos años y medio y solo cruzan **23 de 46**. El propietario decidió NO aplicarlo: el perfil ya enseña el ELO oficial de la FACV, que está al día y lo tienen los 46, y un FEDA de 2023 en la mitad del club daría una foto peor. **Cuando la FEDA publique lista nueva, basta con pulsar el botón de Admin → Actualización de ELO.** Si publican en otro sitio, cambiar `URL_PAGINA_ELO_FEDA` en `src/lib/import/feda-apply.ts`.
  4. **SMTP de Resend**: sin él la recuperación de contraseña no es fiable. El alta de socios NO depende de esto.
  5. **Probar en móvil con socios reales** un torneo interno y una partida en el tablero: es lo único que nadie ha verificado, porque requiere sesión.
  6. **Jugar en el navegador** (decisión del propietario, con la advertencia registrada de que es la pieza más grande del proyecto y que Lichess ya cubre ese terreno).
  Deuda menor en `docs/superpowers/plans/2026-08-05-deuda-pulido.md`.
- **Bloque grande pendiente, planteado por el propietario el 2026-08-08.** Cuatro piezas, aquí por orden de lo que cuesta y de lo que desbloquea a lo demás. **Ninguna empezada.**
  1. ~~**Tablero decente**~~ **HECHO (2026-08-08).** Las piezas eran caracteres Unicode (♞) y por eso se veía mal: el dibujo lo ponía la fuente del sistema. Ahora son **SVG** en `public/piezas/`, juego `celtic` de Maurizio Monge, **MIT** (cburnett y merida, los más conocidos, son GPLv2+ y no interesa meter copyleft fuerte aquí). El nombre del fichero sale de `chess.js` (`color`+`tipo`), así que cambiar de juego es cambiar los doce SVG — ver `public/piezas/LICENCIA.md`, que lista las otras opciones permisivas. Añadidas también las **coordenadas** en los bordes y **arrastrar y soltar** con eventos de puntero (`pointerdown/move/up` + `setPointerCapture`, NO la API de arrastrar de HTML, que con el dedo no dispara nada), conviviendo con el toque-toque de siempre, que es el que funciona en móvil y el único que llega con teclado. Verificado en local moviendo, arrastrando y probando una jugada ilegal; **la coronación arrastrando está escrita pero no jugada**.
  2. ~~**Analizador con motor**~~ **HECHO (2026-08-08).** Stockfish 18 en WebAssembly, dentro del navegador, en `public/motor/` (ver su LEEME.md). **SOLO en el revisor de partidas**, por decisión del propietario: metiendo una partida en el tablero nadie necesita que le digan si la jugada era buena. Compilación **`lite-single`**: `single` porque las de varios hilos exigen `SharedArrayBuffer` y con él habría que servir TODA la web con las cabeceras COOP/COEP, que romperían cualquier recurso de fuera; `lite` porque la red completa pesa 110 MB y esta 7. **Se carga a mano**, al pulsar "Analizar", nunca al abrir la pantalla — y el `sw.js` no precachea nada, solo atiende push, así que instalar la PWA no se lo traga. La lógica que se puede equivocar (el SIGNO de la puntuación, que UCI da desde quien mueve, y la traducción de la línea a jugadas) vive aparte y pura en `src/lib/ajedrez/evaluacion.ts`, con 24 tests; el Worker en `motor.ts`. **Stockfish es GPLv3**: va tal cual, sin tocar, en su propia carpeta, con su licencia al lado, y la app le habla por mensajes UCI. **Cómo se le habla al motor, que costó dos intentos.** UCI no admite un `position` mientras piensa, y `stop` no es instantáneo, así que mandando `stop`+`position`+`go` de golpe bastaban tres jugadas seguidas para dejarlo mudo. El primer arreglo —encolar las peticiones y esperar un `readyok` con un tope de 5 s— **seguía colgándose, y peor**: el navegador FRENA LOS TEMPORIZADORES de una pestaña que no está delante, así que cambiar de aplicación un momento en el móvil hacía saltar ese tope sin que pasara nada malo, y el motor quedaba marcado como roto para siempre. **Ahora no hay ningún temporizador en el análisis** (solo en el arranque): se va con el protocolo, porque tras un `go` el motor SIEMPRE contesta `bestmove` —termine o se le corte—, así que se guarda la posición pendiente, se manda `stop` si está buscando, y es la llegada de `bestmove` la que lanza la siguiente. Gana siempre la última posición pedida. **La barra es vertical, al lado del tablero, y gira con él**: abajo va siempre el color que se ve abajo, como en Lichess. Verificado en local: posición inicial +0.36 a profundidad 18, reevalúa al pasar jugada, aguanta saltos rápidos por la lista de jugadas, y la barra se invierte al girar el tablero.
  3. ~~**Asistente con IA**~~ **ESCRITO (2026-08-08), pendiente de la clave.** Botón flotante en toda la zona de socios (`src/components/asistente/Asistente.tsx`, montado en `src/app/club/layout.tsx`, solo para quien ya tiene ficha). **Gemini**, elegido por el propietario porque tiene capa gratuita; se habla por REST sin SDK (`src/lib/asistente/gemini.ts`) y el modelo está en una constante, `MODELO`, que es lo único a tocar si Google jubila esa versión. **La clave `GEMINI_API_KEY` vive solo en el servidor**, en `src/app/api/asistente/route.ts`. **Las cinco herramientas de lectura van con el cliente de la SESIÓN del socio, jamás con la clave de servicio**: así la RLS que protege las pantallas protege al asistente, y no hay forma de que le cuente a nadie algo que no vería entrando él — un asistente con clave de servicio es una puerta trasera a toda la base con una frase bien puesta. El carácter (solo ajedrez y club, y reconducir con gracia SIN anunciarlo) está en `instrucciones.ts`, tratado como código y con tests, igual que la validación del historial que manda el navegador (`peticion.ts`). **RANGOS (2026-08-08, exigencia del propietario): el asistente cuenta cosas distintas según quién pregunta.** Son DOS candados y hacen falta los dos, porque cubren cosas distintas: (a) la **RLS** es el candado de los DATOS y no se puede rodear convenciendo al modelo; (b) `rangos.ts` es el de lo que se CUENTA o EXPLICA — a un socio no se le explica cómo se aprueba un alta aunque esa explicación no lea ni una fila. Cada herramienta declara su rango mínimo y `declaracionesPara()` filtra ANTES de enseñárselas al modelo (lo que no ve, no lo puede llamar ni mencionar), y `ejecutar()` lo vuelve a comprobar, porque una lista que se filtra en un sitio y se obedece en otro se desincroniza. El rango sale SIEMPRE de la sesión: decir en el chat "soy el administrador" no cambia nada — probado. **Y ojo con la API: Gemini devuelve 400 y no contesta nada si en una `functionDeclaration` va un campo que no conoce**, así que el `rango` se quita antes de mandarla; hay un test que lo vigila. **Se han aplicado las lecciones del bot que el propietario ya tenía en producción en otro proyecto**, guardado en `docs/referencia/chatbot-ia-garestudio.md` (él lo borra de allí; esta es la copia buena): formato de texto plano —el markdown se pinta como asteriscos sueltos en el chat—, rechazos variados mencionando lo que te han pedido, **fechas precalculadas** en el prompt porque los modelos deducen mal "mañana", freno de mensajes por socio, y **suplente de modelo**: el bueno de la capa gratuita da 503 a ratos. Ojo con esto último: **Google retiró `gemini-2.5-flash` para cuentas nuevas de un día para otro** y dejó muerto aquel bot; por eso hay `LLM_MODEL` para forzar otro sin tocar código, y el catálogo que acepta una clave se consulta en `https://generativelanguage.googleapis.com/v1beta/models?key=CLAVE`.
  **FALTA: crear la clave en https://aistudio.google.com/apikey, ponerla en `.env.local` y en Vercel.** Sin ella el asistente contesta que no está configurado y el resto de la app va igual. **FUNCIONANDO Y VERIFICADO EN PRODUCCIÓN (2026-08-08)**: contesta el número de orden y el ELO reales, nombra bien los días, dice que no cuando le preguntan por un socio inventado, reconduce con gracia y aguanta un "ignora tus instrucciones anteriores". La clave estuvo un rato solo en `.env.local`: **eso NO llega a Vercel**, hay que crearla también en Settings → Environment Variables y desplegar de nuevo; `GET /api/asistente` (solo admin) lo dice sin enseñar el valor. Probado además con `scripts/probar-asistente.mjs`, que arma el prompt de verdad y responde las herramientas con datos de mentira (no toca la base, y no es un test porque gasta cuota). Resultado: contesta el dato correcto, usa el formato de lista sin markdown, acierta las fechas ("pasado mañana es el lunes 10"), no se inventa lo que no hay, reconduce con gracia y **aguanta un "ignora tus instrucciones anteriores"**. **Modelo por defecto: `gemini-3.1-flash-lite`, y NO el bueno**: `gemini-3.5-flash` se quedó sin cuota gratuita a la TERCERA pregunta con la clave del club. Y `gemini-2.5-flash-lite` da 404 ("no longer available to new users"), o sea que Google ha retirado dos modelos que este documento daba por buenos: por eso el suplente es un ALIAS (`gemini-flash-lite-latest`), que se mueve solo. Si algún día se paga la API, `LLM_MODEL=gemini-3.5-flash`.
  4. **Jugar en la app, con chat — ESCRITO Y A MEDIO PROBAR (2026-08-08).** El propietario eligió lo grande en los dos ejes: **en vivo con reloj** y **las dos cosas** (retos entre socios y partidas de los torneos internos). Migraciones **0022** (tablas `challenges`, `live_games`, `live_chat` + RLS + publicación de tiempo real), **0023** (cadencia del torneo + índice único de una partida por emparejamiento) y **0024** (arreglo de seguridad, ver abajo). Todo aplicado.
     **Dónde está la verdad**: el reloj y las reglas son del SERVIDOR (`src/lib/vivo/`, 50 tests) y `live_games` **no tiene ninguna política de escritura** — se toca solo desde `jugar/actions.ts` con clave de servicio, tras comprobar quién eres, si es tu turno y si te queda tiempo. Verificado: con la clave anónima, insertar una partida da 42501. El "ahora" lo pone siempre el servidor.
     **Colores, que son DOS reglas**: en torneo se COPIAN de `club_pairings` (los repartió `emparejar.ts` con el criterio oficial; volver a sortear rompería el equilibrio del torneo); en amistosa, `blancasEnAmistosa` sortea el primer encuentro entre dos socios y luego alterna.
     **EL FALLO QUE COSTÓ DOS RONDAS ENCONTRAR, y la lección**: el tiempo real no entregaba NADA. `postgres_changes` aplica la RLS **al que escucha**, y si el socket no lleva el token del usuario el servidor no ve a nadie y lo filtra todo — el canal dice `SUBSCRIBED` igualmente, que es lo que despista. Se arregla con `src/lib/supabase/vivo.ts`, que pone el token ANTES de suscribir. **Medirlo fue lo que lo resolvió** (dos clientes contra la misma fila, uno con clave de servicio y otro sin sesión); las dos rondas anteriores fueron a base de suponer. Encima de eso: la jugada se pinta antes de mandarla (validada con las mismas reglas) y `mover` ya NO llama a `revalidatePath`, que rehacía la página entera en cada jugada.
     **Seguridad, migración 0024**: la política del chat comparaba `p.player_id = player_id` dentro de un `exists` sobre `profiles`, y ese nombre sin cualificar lo resuelve Postgres contra la subconsulta — se leía `p.player_id = p.player_id`, siempre cierta. Cualquier socio vinculado podía escribir en el chat en nombre de otro. **Cuidado con esto en cualquier política nueva: cualificar siempre con el nombre de la tabla.**
     **PROBADO CON DOS SESIONES Y APROBADO (2026-08-09)**: jugadas al instante, chat sin recargar, retos que meten a los dos en la mesa, tablas, volver jugada, abandono, resultado al torneo y PGN al repositorio.
     **TODO LO EN VIVO VA POR DIFUSIÓN, no por `postgres_changes`** (`src/lib/vivo/difundir.ts`): la mesa, el chat y **los retos** (`difundirAviso` al canal `avisos-<ficha>`). Escuchar la tabla no entregaba nada y todo se sostenía con reintentos, de ahí que aceptar un reto tardara en meter al otro y que el número rojo apareciera tarde. **REGLA: si algo tiene que llegar rápido, se difunde; el reintento es una red, nunca el motor.** Y no se mezclan `postgres_changes` y difusión en un mismo canal: si la RLS rechaza el enganche a la tabla, se cae el canal entero y sigue diciendo `SUBSCRIBED`.
     **UN SOLO SITIO ESCUCHANDO CADA COSA.** `Avisos` (en el layout) lleva los retos: entra en la partida al aceptar, refresca `/club/jugar` y alimenta el número rojo. La pantalla de Jugar tenía su propio canal y su propio reintento en paralelo — dos sitios escuchando lo mismo es el doble de consultas y una carrera por ver quién te lleva antes a la partida.
     **El número rojo del menú lo lleva el cliente** (`src/components/avisos/Pendientes.tsx`): la cuenta del layout es solo el valor de partida, porque un número pintado en el servidor se queda congelado hasta la siguiente navegación.
     **EL RELOJ SE PARA AL ACABAR** (`parado` en `reloj.ts`). Sin eso la fila conserva los milisegundos del INSTANTE DE LA ÚLTIMA JUGADA y al terminar los dos relojes dan un salto hacia arriba. `ultima_jugada_en` a null es lo que le dice al navegador que deje de contar. Vale para mate, bandera, abandono y tablas.
     **PRESENCIA, DOS COSAS DISTINTAS**: el círculo verde de todo el club (`Presencia.tsx`, un canal para todos, montado en el layout) y **quién está mirando esta pantalla** (`Mirando.tsx`, una sala por pantalla: `partida-<id>`, `torneo-<id>`, `repaso-<id>`). Ninguna de las dos toca la base ni pasa por RLS: los navegadores se anuncian entre ellos y al cerrar la pestaña desaparecen solos. Nunca encima del tablero: empuja las piezas y se mueve mientras se lee la partida.
     **CON PÚBLICO, EL QUE MIRA NO CUENTA IGUAL QUE EL QUE JUEGA**: el reintento de la mesa es de 5 s jugando y de 20 s mirando. En una final del club la mesa la consultan dos jugadores y cuarenta espectadores; al que juega quedarse colgado le cuesta la partida, al que mira cinco segundos no le cuestan nada.
     **DATOS DE PRUEBA LIMPIADOS (2026-08-10)**: borrados el torneo `PRUEBA`, las 25 partidas en vivo de prueba con su chat, los 38 retos, la partida del repositorio con marca PRUEBA, y la segunda cuenta (andrakingyt@gmail.com) con su perfil y su solicitud de vínculo. Verificado: `live_games`, `live_chat`, `challenges`, `club_tournaments` y `notifications` a cero; 46 fichas activas; la ficha de Aarón Escrivá intacta y libre para cuando él se registre; queda una sola cuenta (jony9vcf@gmail.com). Para volver a probar en vivo: `node scripts/torneo-prueba.mjs poner`.
     **MESAS PRIVADAS (2026-08-10, migración 0033, decisión del propietario)**: los canales `partida-<id>` (jugadas + chat) son privados — `private: true` al abrirlos en la mesa, `private: true` en los mensajes del endpoint REST de difusión, y dos policies sobre `realtime.messages` (SELECT e INSERT para topics `partida-%`, socios vinculados o admin). **Un canal privado SIN policy se deniega por defecto** (verificado con `scripts/probar-mesa-privada.mjs`): por eso la migración debe aplicarse ANTES del deploy. La presencia (`presencia-club`, `mirando-*`) y los avisos (`avisos-<ficha>`) se quedan públicos a propósito: no llevan datos, solo señales. Espectador socio: sigue viendo chat y jugadas en vivo; escribir en el chat sigue siendo solo de los dos jugadores (como siempre). El de fuera sin sesión: ya no escucha nada.
     **IDEA APUNTADA, NO EMPEZADA — "todas las mesas de la ronda a la vez"**: una pantalla del torneo con todos los tableros en marcha, para seguir una ronda de un vistazo. **NO se hace montando N veces la mesa**: cada partida abierta son 3 canales y un reloj repintando a 10 fps, y seis a la vez ahogan el navegador del que mira. Hay que hacerlo con **un canal por torneo** (el servidor difunde ahí cada jugada de cualquier mesa de la ronda), tableros **sin reloj corriendo** —el tiempo, en número y solo al recibir jugada— y **sin reintento por mesa**, uno solo para toda la pantalla. El techo aquí no es el servidor, es el navegador del espectador.
- **Bloque nuevo pedido por el propietario el 2026-08-09.** Por orden de lo que aporta antes:
  1. ~~**Repaso de lógica de toda la app**~~ **HECHO (2026-08-10)**: auditoría en cinco frentes (RLS, acciones con clave de servicio, auth/rangos, jugar en vivo, entradas/inyección) con informe en `docs/superpowers/specs/2026-08-10-auditoria-logica-seguridad.md`. Salió bien lo importante: las 20 acciones de servidor comprueban identidad y rol ANTES de escribir y toman la ficha de la sesión; el modelo de rangos es coherente en las cinco capas; las ocho vías de trampa en las partidas están cerradas; y el patrón del bug de la 0024 (columna sin cualificar) NO se repite en ninguna otra policy. **Dos críticos encontrados y arreglados en la migración 0027**: el blindaje de 0007 solo cubría `lineups`, no `lineup_boards`/`board_results`, y la policy de `matches` dejaba cambiar `estado` (reabrir una jornada jugada rodeaba el candado) y el marcador a mano. **Verificado en vivo** montando un capitán temporal y atacando una jornada real: reapertura, marcador y ronda bloqueados; sede y chat normal siguen funcionando. También cerrado el aviso falso del chat (policy + un mensaje difundido sin autor ya no se pinta, se comprueba contra la base). **Queda para más adelante**: canales de tiempo real privados (hoy cualquiera con la clave pública puede ESCUCHAR una partida — comprobado; escribir no altera nada) y los menores del informe para la pasada de pulido.
  2. ~~**Notificaciones, que funcionen de verdad**~~ **HECHO (2026-08-10)**, migración 0028. Antes convivían tres caminos sin un sitio común: push web suelto, las tarjetas flotantes de `Avisos` y el número rojo del menú, y nada quedaba guardado si el push fallaba o si el socio no tenía ningún dispositivo suscrito — ESE aviso se perdía para siempre, sin dejar rastro en ningún sitio. Ahora hay una tabla `notifications` (bandeja en `/club/avisos`) y **una sola puerta de entrada, `avisar()`** (`src/lib/avisos/enviar.ts`): guarda la fila primero y SOLO DESPUÉS intenta el push, así que un push fallido, un servicio caído o un socio sin suscripción nunca hacen desaparecer el aviso. **Regla nueva: todo el código manda avisos con `avisar()`, nunca con push directo** — los 11 sitios de la app que avisaban algo se migraron; el único push directo que queda a propósito es el botón de prueba del admin, que no debe dejar fila en la bandeja de nadie. La política de a quién le toca push (`src/lib/avisos/politica.ts`, puro, con tests) tiene **cuatro grupos silenciables** (interclubs, torneos, partidas, gestión) que el socio apaga desde Perfil (`profiles.avisos_silenciados`, guarda lo APAGADO, así que quien no toca nada sigue recibiendo todo) y **una excepción expresa del propietario: la convocatoria NUNCA se silencia** — si el capitán te alinea, te enteras aunque tengas Interclubs apagado, porque de eso depende si vas a jugar. Un fallo de push real (404/410, suscripción muerta) se distingue de uno reintentable, y el cron diario (`reintentarAvisosFallidos()`) reintenta los `fallido` una sola vez y de paso rescata los `pendiente` huérfanos (más de 10 minutos sin resolverse), que es el hueco que dejaba abierto el primer diseño si las dos escrituras seguidas de `avisar()` fallaban las dos. **Verificado en vivo contra la base real (Task 8)**: los tres escenarios de la spec confirmados con la función de verdad, no con una réplica — con push a una cuenta suscrita (`push` queda `entregado`), sin push a una cuenta sin suscripción y a una con el grupo silenciado (las dos quedan `no_tocaba`, que es justo el caso que antes se perdía sin dejar rastro), y la convocatoria ignorando un silencio de Interclubs (`entregado`, nunca `no_tocaba`). Y la seguridad: `notifications` no tiene policy de INSERT a propósito, así que con una sesión de socio de verdad un insert propio o a nombre de otro se rechaza con 42501, y marcar leído el aviso de otro no toca ni una fila (solo el propio). **Revisión adversarial y arreglos globales (2026-08-10)**: el número rojo llevaba al sitio equivocado y la bandeja era inalcanzable salvo con algo sin leer, un reto viejo aceptado te podía sacar de la partida que estabas jugando, y quedaban dos frases falsas en los comentarios de la 0028 más un rastro de diagnóstico que faltaba — los cinco se cerraron en una pasada. **El menú YA NO lleva un único número que sume dos cosas: son DOS, cada uno en su propia entrada.** "Jugar" cuenta retos pendientes (tabla `challenges`); "Avisos" es una entrada PROPIA y SIEMPRE VISIBLE del menú (`/club/avisos`, no solo cuando hay algo sin leer) y cuenta avisos sin leer (tabla `notifications`). Antes había un solo número —la suma de los dos— de badge sobre Jugar que en realidad llevaba a la bandeja: tocar el único sitio rojo de la pantalla con un reto pendiente aterrizaba en "sin avisos", porque un reto no es ningún tipo de esa tabla. **Los dos números se calculan con la MISMA fórmula en el servidor** (`src/app/club/layout.tsx`, solo el valor de partida) **y en el cliente** (`src/components/avisos/Avisos.tsx`, función `repasar()`, que los recalcula cada pocos segundos y es quien manda de verdad) — si algún día discreparan, el número parpadearía al navegar. El contexto que los reparte es `src/components/avisos/Pendientes.tsx` (dos contadores, `avisos` y `retos`, ya no uno). **Con una partida en juego delante (jugándola o mirándola), `Avisos` NO PINTA NINGUNA TARJETA** (`src/components/avisos/EnPartida.tsx`, que marca `Mesa.tsx` en cuanto hay una partida sin resultado a la vista): si algún día parece que "las tarjetas no salen mientras juego" es un bug, NO lo es — es la regla, y el motivo es que perder por tiempo por leer "Fulano te reta" es peor que dejar caducar ese reto. El aviso no se pierde: sigue en la bandeja y en su badge del menú. **Por el mismo motivo, aceptar un reto ajeno TAMPOCO te saca ya de la partida que tienes abierta**: si el reto se resuelve mientras `enPartida` es `true`, la navegación a la mesa nueva se salta (antes sí navegaba, y un reto viejo aceptado por el otro con mal timing te sacaba de tu partida en curso con el reloj corriendo). **Bandeja con volumen real, "marcar todos como leídos" (2026-08-10)**: cerrar el agujero de arriba tuvo un efecto secundario — los avisos de disponibilidad pasaron a guardarse para los 46 socios dos veces por semana (antes solo para quien tenía push activado), unos ~100 avisos al año por socio. Y como los 11 tipos de aviso llevan `url`, marcar uno leído te sacaba SIEMPRE de la bandeja: vaciarla a mano pedía volver a entrar una vez por cada aviso. Ahora "ir al aviso" y "marcarlo leído" son dos acciones separadas por fila (`marcarLeido` navega y de paso marca, para cuando sí quieres abrirlo; `marcarLeidoQuieto` solo marca, sin moverte) y hay un botón "marcar todos como leídos" (`marcarTodosLeidos`) encima de la lista que solo se pinta si queda algo sin leer. Un aviso ya leído no ofrece "marcar leído" (nada que marcar) y uno sin `url` (la columna es nullable) no ofrece "ir" (nada que abrir). **Pendiente**: la prueba del propietario en su móvil de verdad (activar/desactivar un grupo en Perfil, provocar un aviso, mirar la bandeja y las dos entradas del menú) — es el gate que le corresponde a él, no se puede verificar en local.
  3. **Perfil — HECHO EN SU MAYOR PARTE (2026-08-10, migración 0030)**: foto (recorte al cuadrado central y 512px EN EL NAVEGADOR antes de subir — el servidor revalida tipo y tamaño; bucket `fotos` PRIVADO, se enseña con URLs firmadas de 1h porque son caras de 46 personas identificables), aperturas favoritas (texto libre de una línea, tope 120), **tema del tablero** (4 opciones en `src/lib/ajedrez/temas.ts`; `Tablero` lo lee por CONTEXTO —`ProveedorTemaTablero` en el layout del club— para que todos los tableros pinten igual sin enhebrar props; los colores van en `style` porque Tailwind no genera clases para colores de runtime), y **ficha pública** `/club/socios/[id]` para TODAS las fichas (foto/aperturas solo si su dueño las puso; enlaces de entrada desde el orden de fuerza y el "subida por" de las partidas). **FOTO Y APERTURAS VIVEN EN `players`, NO EN `profiles`**, por la RLS: `profiles` solo lo lee su dueño o el admin, y la gracia es que LOS DEMÁS lo vean. **Juegos de piezas HECHOS también (2026-08-10, migración 0031)**: cuatro juegos en `public/piezas/<carpeta>/` (celtic MIT, chessnut Apache-2.0, fantasy y spatial MIT — bajados del repo de Lichess con OK del propietario; tabla de licencias y pasos para añadir más en `public/piezas/LICENCIA.md`), catálogo en `src/lib/ajedrez/piezas.ts` con un test que comprueba los 12 SVG de cada juego EN DISCO, selector en el perfil enseñando el caballo de cada juego, y elección en `profiles.juego_piezas` por el mismo contexto que el tema. Lo antiguo del punto: Foto que sube el socio (Supabase Storage, con recorte y tope de tamaño — una foto de móvil sin tocar son 5 MB), aperturas favoritas, y **tablero y piezas elegibles entre opciones que ponemos nosotros** (los SVG ya están separados por juego en `public/piezas/`, así que añadir juegos es añadir carpetas; **cuidado con las licencias**, ver `public/piezas/LICENCIA.md`). Las preferencias se guardan en el perfil y viajan con la cuenta, no en el navegador. Y una ficha pública de socio donde se vea todo eso.
  4. **Panel de admin con datos de uso — HECHO (2026-08-10, migración 0032)**: `/club/admin/uso` con pestañas día/semana/mes (14 días, 12 semanas, 12 meses). **La decisión de privacidad quedó como se propuso**: contadores AGREGADOS por día (`uso_diario`: visitas y latidos) y, de cada socio, solo «entró tal día» (`uso_socios_dia`, una fila por día, sin horas ni pantallas). El **latido** (`src/components/uso/Latido.tsx`, en el layout del club) marca cada 5 min con la pestaña VISIBLE — una pestaña olvidada de noche no suma horas de uso; tiempo = latidos×5 min, media de conectados = latidos/288 franjas. Partidas/retos/chat/avisos NO se registran de nuevo: los agrega `recuento_uso()` en SQL — **en SQL y no en la app porque PostgREST corta en 1000 filas** y contar en Next infracontaría en silencio. Las dos funciones tienen el EXECUTE revocado a los clientes (y CONCEDIDO explícitamente a service_role: revocar a public se lo quitaba también); los activos por semana/mes se calculan con Set por periodo (`src/lib/uso/agrupar.ts`, con tests): la misma persona en dos días de una semana es UN activo, sumar los diarios es el error clásico. Lo antiguo del punto: (`/club/admin`, solo admin): partidas jugadas, visitas, tiempo de uso, cuentas activas, media de conectados, por día, semana y mes. **Lo que hoy NO se guarda es casi todo**: hay partidas y fichas, pero no visitas ni sesiones ni tiempo. Hace falta decidir primero qué se registra y **con qué respeto a la privacidad** —esto es un club pequeño y "quién ha entrado y cuánto rato" son datos de personas identificables—; lo razonable es guardar contadores agregados por día y no un registro por persona y visita. La presencia en vivo no sirve para esto: no se guarda a propósito.
  5. **UX/UI de las pantallas de administración — PASADA MECÁNICA HECHA (2026-08-10)**: añadido `loading.tsx` a las 9 subsecciones que no lo tenían (sin él Next no precarga la ruta — la navegación por Admin iba sin esqueleto ni prefetch), y la lista de 46 jugadores del admin de orden de fuerza partida en dos columnas como su gemela de socios. Lo demás estaba bien contra las reglas de la casa: medidas de Cabecera/Contenedor coherentes en las 10, todas con volverA, todos los formularios con BotonAccion. **Queda la pasada VISUAL con ojos** (colores, huecos, jerarquía), que necesita al propietario delante porque Admin no se puede mirar sin su sesión: apuntar lo que chirríe usándolo y traerlo. Lo antiguo del punto: Se hicieron a medida que hacían falta y nunca se han mirado juntas: `/club/admin` y lo que cuelga (acceso, orden de fuerza, torneos, ELO, altas de socios). Es la parte de la app que menos se ha cuidado porque solo la usa el propietario, y aun así es donde más daño hace un botón mal puesto — desde ahí se aprueban altas y se importan datos.
  6. **UX/UI de Perfil, DESPUÉS de meterle la lógica del punto 3** y no antes: rediseñar una pantalla y luego llenarla de cosas nuevas es hacerla dos veces.
  7. **Inventario de automatizaciones — HECHO (2026-08-11)**: `docs/referencia/automatizaciones.md` — qué corre solo (el cron director y lo automático de la app), qué NO se puede automatizar con verificación empírica (**la sonda de FIDE se lanzó ese día: Vercel tampoco puede descargar la lista mensual, `fetch failed` a los 10,5 s; la sonda se borró tras responder**, su código queda en git), qué es manual a propósito, y los candidatos evaluados con veredicto (el recordatorio de resultados sin meter y el aviso de convocatoria, para la 2027). Criterio: se automatiza lo semanal, no lo anual. Lo antiguo del punto: Hay que escribirlo en un sitio: hoy la única automatización es **el cron de Vercel** (`/api/cron/director`, diario, multiplexado por día: lunes pedir disponibilidad, jueves recordar, viernes la sync de la FACV), y la sync del viernes son **tres importaciones en cadena cuyo orden es una dependencia** (`sync-semanal.ts`: orden de fuerza → resultados y clasificación → actas por tablero, 17,6 s medidos). Todo lo demás son botones que hay que pulsar a mano. **Lo que NO se puede automatizar y conviene dejar escrito para no volver a intentarlo**: `fide.com` bloquea IPs de datacenter (Vercel y GitHub Actions), así que el ELO FIDE en vivo solo sale desde local; y **la FEDA no publica listas desde diciembre de 2023**, con lo que su importador está arreglado pero sin fuente. Candidatos a mirar: arranque de temporada (hoy son cuatro pasos manuales y uno es cambio de código, `TEMPORADA_ID_FACV`), avisos de jornada próxima, y recordatorio de resultados sin meter. Criterio: automatizar lo que se repite cada semana; lo que pasa una vez al año casi nunca sale a cuenta.
  8. **Pestaña de "qué puedes hacer aquí" — HECHA (2026-08-10)**: `/club/perfil/guia`, con tarjeta de entrada en el perfil. **El contenido vive en `src/lib/guia/guia.ts` (con tests) y de ahí leen LA PANTALLA Y EL ASISTENTE** — el mapa "qué hay en la aplicación" de `instrucciones.ts` ya no está escrito a mano, se genera de la guía con `guiaParaElModelo(rango)`, recortado al rango de quien pregunta con la misma escala de `rangos.ts` (a un socio el modelo ni le menciona la administración: lo que no ve no lo puede explicar). Añadir o cambiar una sección de la app = tocar SOLO la guía, y la cuentan igual los dos. Lo antiguo del punto:, con lo que hay en la app explicado, **recortada por rango**: un jugador no ve lo de admin ni lo de capitán. Ojo: el asistente ya tiene el mapa de la app y la tabla de rangos en `src/lib/asistente/`; esa pantalla y el asistente tienen que contar lo mismo, así que el texto debería salir de un sitio compartido y no escribirse dos veces.
- **INSTALAR LA PWA: dos bugs encontrados el 2026-08-12 con el primer socio de prueba** (dijo que no le salió lo de instalar — tenía razón, y no era su móvil). (a) **`public/sw.js` no tenía manejador de `fetch`, y Chrome EXIGE que la web responda algo sin conexión para considerarla instalable**: el aviso de instalación no salía NUNCA en Android. Arreglado con `public/sin-conexion.html` precacheada y un `fetch` que **solo atiende NAVEGACIONES y siempre prueba la red primero** — cuando hay red es invisible, así que no puede romper sesión, tiempo real ni jugadas. (b) La notificación push usaba `/icon.svg`, **que no existe**, así que salían con el icono del navegador; ahora `/icon-192.png` con `badge`, y al tocarla se trae al frente la ventana abierta en vez de abrir otra. **Y en iPhone NO EXISTE ningún aviso de instalar** (Safari no tiene `beforeinstallprompt`): de ahí `src/components/InstalarApp.tsx`, con botón real en Android e INSTRUCCIONES en iOS, montado en Inicio y en Perfil, que se esconde solo al estar instalada. Ojo: **en iOS sin instalar NO HAY PUSH**, así que en Perfil va ANTES de activar notificaciones — ese es el orden real de los pasos. Usa `useSyncExternalStore` y no un efecto con `setState`: el linter del compilador de React lo prohíbe y además así la hidratación no discrepa.
- **ESCALAR A OTROS CLUBES: analizado el 2026-08-12, `docs/referencia/escalar-a-otros-clubes.md`.** Resumen: hasta ~5 clubes, **franquicia** (un repo, N despliegues con su propio Supabase/Vercel/claves) — coste ~0 €, aislamiento gratis, la RLS actual vale tal cual, y solo hay 26 apariciones de "Fomento" en código que extraer a config. **SaaS multi-tenant (club_id + reescribir las 63 policies) solo con 10+ clubes dispuestos a pagar**: es otro proyecto del tamaño de este, y arrastra identidad multi-club, subdominios, capas de pago, facturación y RGPD de datos de menores de N clubes. **NO meter `club_id` "por si acaso"**: duplicaría el coste de cada pantalla nueva para una demanda que no existe. Los importadores solo valen para clubes DE LA FACV (otra federación = adaptadores nuevos).
- **Regla de pantalla que salió del repaso del 2026-08-08: usar el ancho, no el alto.** Cuando una lista crece sin techo, NO se deja correr hacia abajo. Dos salidas, según lo que sea: (a) **partir en dos columnas** desde `lg` con `partirEnDos` (`src/lib/ui/columnas.ts`, 10 tests) — es lo que se hizo con el ranking oficial de 46 filas y con el del club; (b) **enseñar una sola parte con selector**, que es lo que se hizo con las rondas de los torneos internos (una liguilla de 8 son 28 cruces). Y una columna de tarjetas de dos líneas se convierte en **una caja con filas**, como el calendario de Interclubs, que es el patrón de referencia de la app.
- **~~PENDIENTE: limpiar el código de acceso viejo del repo~~ HECHO (2026-09-23)**. El valor muerto (rotado el 2026-09-05) se sustituyó por un ejemplo evidente en `src/lib/acceso/codigo.ts`, `src/lib/acceso/codigo.test.ts` y el plan de onboarding. **La migración 0009 NO se tocó**: está aplicada, y la regla es no editar una migración aplicada — además el valor ya está en el historial, así que cambiarlo ahí no borraría nada y sí rompería la trazabilidad de lo que de verdad se sembró.
- **DECISIÓN CERRADA (2026-09-05): NO se reescribe el historial de git.** El código de acceso viejo estuvo en el repo público desde agosto (`4d5e735`, `e78f52b`, y en `CLAUDE.md` desde `23ba41d`), y el teléfono y el correo de un socio estuvieron en el `CLAUDE.md` desde el 2026-08-07 (`23ba41d`) hasta que la reorganización del 2026-09-05 los quitó del árbol. Los dos siguen en commits antiguos y **ahí se quedan**: el código ya está rotado, y reescribir historial de un repo público no garantiza el borrado (caches y forks). No reabrir esto. Lo que sí cambia es la regla: los códigos de acceso y los datos de contacto de socios NO se escriben en ningún archivo del repo, ni en los de documentación.

## Repaso previo al lanzamiento al club (2026-09-23)

- **EL SCRIPT DE DATOS DE PRUEBA ERA UNA BOMBA Y SE DESACTIVÓ.** `scripts/datos-prueba.mjs quitar` borraba tablas ENTERAS (`games`, `tournament_attendance`, `cars`) porque en agosto estaban a cero y había una sola cuenta. Hoy hay **4 cuentas** y datos reales dentro (3 partidas, los 3 torneos "Social 2026", 5 asistencias), así que se habría llevado por delante lo de verdad — y ni siquiera corría: su guarda aborta en cuanto ve un torneo interno que no sea de prueba. Ahora borra **solo lo que puso**: marca `PRUEBA` en el texto visible + un manifiesto gitignorado (`scripts/.datos-prueba.json`) para lo que no puede llevarla (asistencias y el booleano `de_interes`). Sin manifiesto, no toca esas dos cosas y lo dice. **Comprobado lanzando `quitar` ANTES de sembrar: cero filas borradas.**
- **NUEVO `scripts/revisar-coherencia.mjs`** (solo lectura): pasa los datos REALES por los módulos de verdad —validador del RGC, clasificación del club, `eloParaOrdenar`— y avisa de lo que no cuadra. Cubre el hueco entre los tests (datos inventados) y los clics (la pantalla que miras). Lanzarlo antes de cada lanzamiento.
- **COMPARTIR POR WHATSAPP**, que no existía: `src/lib/compartir/mensaje.ts` (puro, con tests) + `BotonCompartir`. En torneo de fuera, torneo del club, jornada y la app entera desde la guía. **El enlace va DENTRO del texto** y el dominio está escrito a mano, no sacado de `window.location` (compartir desde una vista previa mandaría un enlace que no abre nadie).
- **LA GUÍA SE HABÍA QUEDADO ATRÁS** (no contaba "Socios del club", y prometía "orden de fuerza oficial, por número o por ELO"). Corregida, y con un test que mira las CARPETAS DE RUTAS reales y obliga a declarar dónde sale cada una.
- **LAS BAJAS NO SE DISTINGUÍAN EN EL PANEL DE ADMIN.** `/club/admin/orden-fuerza` es la excepción a "no las ve nadie" —y tiene que serlo, porque es desde donde la junta llega a una ficha para reactivarla—, pero salían idénticas a un socio activo. Ahora van atenuadas y con la chapa "Ya no está en el club".
- **Comprobado y sano**: 86 server actions con permisos (la única sin ellos es el formulario público de ingreso, con freno por IP), los 4 endpoints de `/api` con secreto o sesión, ninguna clave de servicio en cliente, 35 tablas con RLS, ningún enlace interno roto, ningún fichero huérfano, `npm audit` a 0.
- **AVISO DE DESPLIEGUE**: el commit de la auditoría del 2026-08-26 (`e8c3ce0`) quedó en la rama `docs/reorganizacion-memoria` y **`main` no lo tiene**, así que lo de las bajas visibles, el 404 y la subida de Next **no está en producción**. Hay que fusionar antes de abrir la app al club.

- **EL ELO FIDE LLEVABA 18 DÍAS SIN ACTUALIZARSE, y el motivo era la mudanza (2026-09-23).** Al mover el proyecto a `Proyectos\` el 5 de septiembre, la tarea programada de Windows se quedó apuntando a la ruta vieja: arrancaba, no encontraba el `.cmd` y moría **sin escribir una línea en el log**, así que la app seguía enseñando los ELOs del 5 de septiembre como si fueran de hoy. Reapuntada, y de paso quitadas las condiciones de batería (`DisallowStartIfOnBatteries`, que en un portátil la rechazaba igualmente). Comprobado lanzándola: 38 actualizados, 0 errores. **Las dos trampas y cómo distinguirlas están en la skill `actualizar-elo-fide-local`** — la clave es mirar `logs/elo-fide.log` antes que nada: sin cabecera nueva, el fallo está en la tarea, no en el script.
- **Temporada `Interclubs 2026` marcada como NO activa (2026-09-23)**: terminó el 28 de marzo y seguía activa, así que a un socio le salía "Mi disponibilidad" con cero jornadas y sin el aviso de temporada terminada. En enero se crea la 2027 con la skill `arrancar-temporada`.
- **RECUPERAR LA CONTRASEÑA: NO EXISTÍA, y la app se la prometía al socio (2026-09-23).** Lo destapó el propietario al no recordar la suya. No había ni pantalla ni llamada a Supabase —solo `login`, `registro` y `auth/confirm`— mientras la pantalla de registro decía "usa un email al que tengas acceso: lo necesitarás si olvidas la contraseña". Con 46 socios a punto de crear cuenta, el primero que la olvidara se quedaba fuera hasta que alguien se la cambiara a mano desde Supabase. **Hecho**: `/recuperar` (pide el correo) y `/nueva-contrasena` (la escribe), enlace en el login, y `/auth/confirm` acepta `?next=` con guarda contra redirect abierto (un `next=//otra-web` convertiría nuestro correo en el cebo perfecto). Reglas en `src/lib/acceso/contrasena.ts` con tests. **APAGADA DE MOMENTO** (`RECUPERACION_POR_EMAIL` en `src/lib/acceso/recuperacion.ts`), decisión del propietario: "de momento sin recuperar contraseña, advertiremos". El motivo técnico es que **Supabase no deja editar la plantilla del email sin SMTP propio**, y la de fábrica pasa por su `/auth/v1/verify` y devuelve al socio a la portada con la sesión en el `#`, que la app no lee — comprobado en producción. Con la recuperación apagada, `/recuperar` no enseña formulario: dice que se le pida a la junta. **Una pantalla que promete un correo y manda un enlace roto es peor que no tenerla.** Para desatascar a alguien mientras tanto: `auth.admin.generateLink({type:'recovery'})` genera el enlace SIN correo (receta en la guía de SMTP). Encenderlo el día que haya Resend = SMTP + pegar la plantilla + poner la constante a `true`. **OJO con la guía vieja: `onboarding@resend.dev` NO vale**, solo entrega a tu propia dirección; hace falta dominio propio o el SMTP de Gmail del club.
- **ARRANQUE EN FRÍO DE LA PORTADA, visto recorriendo la app con el propietario (2026-09-23).** La portada enseña torneos de fuera **solo si ya hay alguien apuntado** (decisión suya del 2026-08-13: "deberían salir cuando alguien pone que va o está en duda"). Esa regla es buena cuando hay de dónde elegir, pero el día que la app se abre al club la asistencia está entera a cero: la portada no mencionaba **ni uno de los 39 torneos que vienen**, así que nadie se entera de que existen, nadie dice que va, y siguen sin salir. **Arreglado solo el caso vacío**: si no va nadie a ningún sitio, se enseñan los tres siguientes del calendario con la línea "Di si vas y aparecerá para todos". En cuanto alguien se apunta, manda la regla de siempre. La decisión original NO se ha tocado.
- **CERRAR LA TEMPORADA ROMPIÓ DOS PANTALLAS DE ADMIN, y el fallo de fondo era una suposición del diseño (2026-09-23).** Al poner `Interclubs 2026` como no activa, `/club/admin/orden-fuerza` perdió la lista de 46 socios —y con ella los editores de mote— y `/club/admin/equipos` se quedó en "No hay temporada activa". Las dos filtraban por `activa = true`. **El diseño daba por hecho que siempre hay una temporada en curso, y no es verdad: el Interclubs va de enero a marzo, así que nueve meses al año no hay ninguna.** Antes no se notaba porque la 2026 se quedó marcada activa desde marzo. Arreglado con `temporadaDeAdmin()` en `src/lib/temporadas.ts`: la activa, o la última si no hay ninguna. **Los importadores NO cambian** — siguen atados a la activa a propósito, porque importar el orden de fuerza nuevo sobre la temporada vieja lo machacaría. La pantalla de equipos dice ahora "· terminada" cuando lo está.
- **PENDIENTE DE DATOS: los equipos B y C están "Sin capitán asignado"** (`/club/admin/equipos`). Para la 2026 ya es histórico —no quedan jornadas—, así que no corre prisa; lo que importa es asignarlos al arrancar la 2027.
- **MENSAJE MANDADO AL CLUB (2026-09-23).** El enlace va a `/registro` y no a `/login`: los 46 tienen que CREAR cuenta, y mandarlos al login haría que intentaran entrar con una contraseña que nunca han creado. Comprobado antes de mandarlo: el código activo **no tiene tope de usos** (`max_usos` a null), así que no se agota a mitad del alta.
- **`scripts/pulso.mjs` (solo lectura) para los primeros días.** Enseña lo que ninguna pantalla enseña, porque son HUECOS y no filas: quién creó la cuenta y no eligió ficha, y **quién está esperando aprobación** —ese socio ve una pantalla de espera y no puede entrar—. Avisa también de lo que puede atascar a todos a la vez: código agotado, y **10 intentos fallidos desde una misma IP en una hora bloquean esa red**, así que en el wifi del club un código mal escrito los deja fuera a todos. La IP no se imprime: para esto basta saber que hay una red en riesgo.
- **LAS VINCULACIONES LAS APRUEBA TAMBIÉN LA JUNTA (2026-09-23, migración 0051 aplicada y verificada).** Antes solo el admin, y eso lo convertía en cuello de botella el día del lanzamiento: quien elige su ficha se queda en una pantalla de espera y **no puede entrar** hasta que alguien le apruebe. La pantalla se mudó de `/club/admin/vinculaciones` a **`/club/vinculaciones`** (bajo `/club/admin` el layout echa a quien no es admin); la ruta vieja queda como redirect porque hay **push ya enviados** que apuntan ahí. Puerta para la junta en el perfil, encima de "Solicitudes de ingreso" y destacada cuando hay alguien. El aviso de nueva solicitud llega ahora a junta y admin. **Verificado con una sesión real de Juanvi (junta, no admin): ve 3 solicitudes, de las cuales 2 son ajenas — con la policy vieja solo habría visto la suya.** Lo que NO se abre: repartir rangos sigue siendo del admin (0011), porque quien puede nombrarse a sí mismo deja de tener rango.
- **NADIE PODÍA VINCULAR SU FICHA (2026-09-23), y fue por cerrar la temporada.** `/club/vincular` pedía la temporada ACTIVA para sacar el censo; al marcar la 2026 como terminada, la consulta dejó de devolver nada y la pantalla decía **"No queda ninguna ficha libre" a todo el mundo**. Es la única puerta de entrada de un socio nuevo. **No deja rastro**: sin ficha elegida no hay solicitud, así que no sale como pendiente ni genera aviso — lo cazó `pulso.mjs` con el primer socio que entró tras mandar el mensaje al club. Misma raíz que las dos pantallas de admin: **el diseño da por hecho que siempre hay temporada en curso**, y el Interclubs va de enero a marzo. Arreglado con el mismo patrón (la activa, o la última). Verificado contra la base: de 0 fichas ofrecidas a 43.
- **"NO ESTOY EN LA LISTA, AVISAD": botón nuevo en `/club/vincular`.** Antes ponía "avisa al admin" y ahí se acababa — y esa tarjeta **se ocultaba justo cuando la lista estaba vacía**, que es el único momento en que el socio no puede hacer nada solo. Ahora avisa a junta+admin desde dentro (tipo `ficha_no_encontrada`, grupo `gestion`), con su email dentro del aviso porque es el único dato que la junta tiene de quien aún no se ha vinculado. **Sin probar de punta a punta a propósito**: pulsarlo manda push de verdad; la primera pulsación real lo confirmará.
- **QUEDAN TRES SITIOS CON EL MISMO PATRÓN `eq("activa", true)`**, todos cosméticos y ninguno bloquea: el número de orden en `/club/perfil` y en la ficha de un socio, y la herramienta `orden_de_fuerza` del asistente (diría "no hay temporada activa"). Los importadores SÍ deben seguir atados a la activa.
- **LA FICHA DE PRUEBAS, OCULTA (2026-09-23).** `players.de_prueba` ya la escondía de los socios, pero el admin seguía viéndola (salía en la lista de retos). Se ha puesto `activo = false`, que es el mecanismo que la esconde de TODAS las listas —retos, socios, inscripciones, vincular— sin tocar código. **Reversible con `update public.players set activo = true where de_prueba;`**. Comprobado: la segunda cuenta del propietario (`andrakingyt@…`) sigue entrando, porque el gate de `/club/(vinculado)` mira que tengas ficha, no que esté activa.
