@AGENTS.md

# Chess Club Manager — Fomento de Gandia · Guía de trabajo

App PWA del club de ajedrez Fomento de Gandia (Gandía). Propietario: J. Ribes ("Joan", admin del club, capitán del equipo A, ficha "Joan Martínez Ribes"). Idioma de trabajo: **español**.

## Dónde está todo

- **Estructura de rutas (2026-08-05, retocada el 2026-08-06):** `/` es la **web pública** del club; la app de socios vive bajo **`/club`**. Cinco secciones en la navegación: Inicio, Interclubs (`/club/equipos`, `/club/disponibilidad`, `/club/jornadas`), **Torneos** (`/club/torneos`, con dos pestañas: `/club/torneos/facv` los de fuera y `/club/torneos/interno` los del club), Partidas y Perfil, más Admin. **Torneos y Club se fusionaron el 2026-08-06** por decisión del propietario: para el socio los dos son torneos, y siete pestañas no caben en un móvil. NO se mezclan en una lista con filtro porque lo que haces en cada uno no se parece (en los de fuera dices si vas y se organizan coches; en los del club te inscribes y el resultado cuenta para el ELO interno). `/club/torneos` **redirige** a la pestaña de fuera y existe solo para no romper las notificaciones push ya enviadas, que apuntan ahí. `/login`, `/registro`, `/auth/confirm` y `/api` se quedan en la raíz. El gate de "sin ficha aprobada" es el layout del grupo `/club/(vinculado)`; `vincular` y `perfil` cuelgan de `/club` fuera del grupo. Rumbo completo en `docs/superpowers/specs/2026-08-05-arquitectura-objetivo.md`.
- **Producción:** https://fomento-gandia-chess-swart.vercel.app (Vercel despliega `main` automáticamente)
- **Repo:** https://github.com/AndraKinG/fomento-gandia-chess (cuenta GitHub principal del usuario)
- **Supabase:** proyecto `fomento-gandia-chess` en su cuenta Google secundaria (jony9vcf@...). Cuenta real del usuario en la app: jony9vcf@gmail.com (admin + capitán A)
- **Specs y planes:** `docs/superpowers/specs/` y `docs/superpowers/plans/` (Fases 0, 1A, 1B, 1C completadas)
- **Referencia de dominio:** `docs/referencia/` — reglamento RGC FACV extraído, verificación empírica de ambigüedades, guía SMTP Resend
- **`.env.local` NO está en el repo** (gitignorado): claves de Supabase (nuevas `sb_publishable_`/`sb_secret_`), VAPID pública/privada (deben ser LAS MISMAS en todas las máquinas — las suscripciones push dependen de ellas), `CRON_SECRET`. Al montar el proyecto en otra máquina, copiar el archivo por canal privado; el resto está en el README.

## Cómo trabajamos (reglas del propietario)

1. **Claude NUNCA hace `git push`.** Se commitea en local y se avisa al usuario con un bloque de comandos copy-paste que SIEMPRE empieza por `cd "<ruta del proyecto>"`. Él pushea con su cuenta.
2. **Migraciones SQL**: Claude escribe el fichero en `supabase/migrations/` (numeradas, en orden) y **pega el SQL completo en el chat** en un bloque ```sql para que el usuario lo copie de ahí al SQL Editor de Supabase (decisión del propietario 2026-08-07: NADA de portapapeles/`Set-Clipboard`). Verificar después vía REST que las tablas/columnas existen. Aplicadas hasta ahora: 0001→0021 (todas; verificado contra la BD el 2026-08-07). **Ojo con PowerShell 5.1 al manipular ficheros de texto**: lee UTF-8 sin BOM como ANSI y corrompe los acentos (`ó` → `Ã³`) — para editar ficheros con acentos usar las herramientas de edición de Claude, no round-trips de PowerShell (este propio fichero se corrompió así el 2026-08-06 y se reparó el 07).
3. **Flujo de desarrollo**: brainstorm → spec escrita y aprobada → plan de tareas pequeñas → ejecución por subagentes con revisión (spec + calidad) por tarea, fix-loops hasta aprobar, y revisión global adversarial al final de cada fase (plugin `superpowers` de Claude Code; el ledger local vive en `.superpowers/sdd/progress.md`, gitignorado).
4. **Prioridad lógica sobre pulido** (decisión expresa del usuario): hallazgos visuales menores van al ledger para una pasada de pulido global futura, NO a fix-loops — salvo roturas de layout o accesibilidad grave. La lógica (validador, permisos, datos) mantiene el listón completo.
5. **Gates de usuario**: acciones que requieren sus cuentas (SQL Editor, Vercel, Resend, push de git) se le piden con pasos exactos clic a clic. Es desarrollador junior: instrucciones concretas, sin jerga innecesaria.
6. **Scripts de un solo uso que TOCAN LA BASE: nunca dentro de `src/**/*.test.ts`.** Es el `include` de vitest (`vitest.config.ts`), así que cualquier `npm test` los vuelve a ejecutar. Pasó el 2026-08-07: un fichero temporal que creaba una temporada de prueba se ejecutó otra vez en una pasada de tests y creó una segunda sin que nadie lo pidiera. Para escribir en la base desde local, usar un script en `scripts/` y lanzarlo a mano.
7. **Verificación en vivo con datos reales** siempre que sea posible (la BD compartida tiene el orden de fuerza real de 46 jugadores, calendario y resultados 2026 reales). No borrar/alterar datos reales sin permiso; los datos de prueba se etiquetan claramente (p. ej. rival "PRUEBA - BORRAR").

## Decisiones técnicas clave (no cambiar sin preguntar)

- **Identidad visual**: blanco y azul "gandiblues". Doble tema: claro *Mediterráneo* / oscuro *Azul profundo* (tokens en `globals.css`, en español: `bg-fondo`, `text-tinta`, `text-acento-texto`...). Tablero de ajedrez blanquiazul cuando llegue la Fase 3. **`--tinta-suave` del tema claro es `#556577` por accesibilidad, no por gusto**: el `#64748b` original daba 4.46 de contraste sobre `--fondo` y AA pide 4.50. No aclararlo. Los 18 pares medidos de ambos temas pasan AA (ver `docs/superpowers/plans/2026-08-05-deuda-pulido.md`).
- **Ancho y navegacion (2026-08-06)**: el ancho de pantalla NO se escribe a mano. Se usa `Contenedor` (`src/components/ui/Contenedor.tsx`) con tres medidas segun para que es el contenido: `formulario` (una columna, ensanchar un campo de texto lo empeora), `lectura` (detalle y texto seguido) y `panel` (listas, tablas y rejillas, es el que aprovecha el monitor). **La `Cabecera` tiene que llevar la MISMA medida que el `Contenedor` de su pantalla** o el titulo queda desalineado respecto a las tarjetas. Las listas de tarjetas usan `REJILLA[2]`/`REJILLA[3]` (o el componente `Rejilla`) para pasar de 1 a 2 o 3 columnas. La navegacion es una sola lista en dos formas (`src/components/Navegacion.tsx`): `NavLateral` desde `lg` (1024 px) y `NavInferior` por debajo, montadas en `src/app/club/layout.tsx`. En escritorio la barra abajo es la peor opcion porque el hueco que sobra es horizontal.
- **Velocidad percibida**: cada seccion de `/club` tiene su `loading.tsx` con `Cargando` (esqueleto con la forma de lo que viene). No es cosmetico: sin `loading.tsx` Next **no precarga nada** de una ruta dinamica, y todas las de `/club` lo son. Las acciones de servidor en `<form action>` van con `BotonAccion` (`useFormStatus`), que se deshabilita y cambia de texto; los Client Components ya lo hacen con `useTransition`. Y las consultas de una pantalla van en tandas con `Promise.all`, no en fila: la home pasaba de diez viajes seguidos a Supabase a dos tandas, y usa `sesionActual()` (memoizada con `cache()`, el layout ya la ha llamado) en vez de repetir `getUser` + `profiles`.
- **Dos rankings, y no se llaman igual a proposito**: el **oficial** (`/club/orden-fuerza`, seccion Interclubs) es el ELO de la FACV y el orden de fuerza, con dos ordenaciones (por numero de orden y por ELO) y estadisticas del club; es el que manda en las convocatorias. El **del club** (`/club/torneos/interno/ranking`) es el ELO interno que sale solo de los torneos internos. Cada pantalla enlaza a la otra porque se confunden con facilidad. La ordenacion y las estadisticas del oficial viven en `src/lib/elo/ranking-oficial.ts` con 14 tests: **`force_order.elo_oficial` es NULLABLE** (migracion 0004, sin `not null`), y un null metido en una resta da NaN, que en un comparador deja la lista en cualquier orden sin fallar ni avisar.
- **Fuerza del jugador** = `force_order.elo_oficial` (orden de fuerza oficial FACV, sincronizado de `of_publico.php?id=56`); fallback `max(FEDA, FIDE)` (RGC art. 52.1). IDs de club/temporada FACV en `src/lib/import/facv-config.ts` (actualizar cada temporada).
- **Validador RGC** (`src/lib/validador/`): módulo puro, flag REQUERIDO `permitirInversionDentroMargen` — **estricto (false) por defecto**; dos ambigüedades del reglamento documentadas en `docs/referencia/verificacion-empirica-rgc.md` (0 inversiones en 11 rondas reales de 2026). No relajar sin confirmación FACV del usuario.
- **Publicar convocatoria**: única puerta = server action con re-validación completa + escritura service_role (trigger de blindaje en migración 0007). El cliente valida en vivo solo como ayuda.
- **Acta por tableros (2026-08-06, migracion 0018)**: el calendario de la FACV solo publica el MARCADOR GLOBAL de cada encuentro; el detalle tablero a tablero esta en **chess-results.com**, al que la FACV enlaza con "Alineacion" (un enlace por grupo, `art=3`). El enlace SIN `&rd=` trae las once rondas de golpe, asi que son 3 descargas y no 33. Parser en `src/lib/import/chessresults.ts` (26 tests contra un acta real de 528 tableros), importador en `chessresults-apply.ts`, tabla `match_boards`. **El HTML tiene tablas anidadas** (la celda del nombre es una tabla, para el cuadradito del color): hay que aplanarlas antes de recorrer filas, y cada fila hay que cortarla en su `</tr>` — sin eso, el ultimo tablero de la pagina se queda sin resultado en silencio. Se guarda todo **desde nuestro punto de vista** (como `board_results` y `games`), invirtiendo el resultado cuando somos visitantes. NO reutiliza `lineups`/`board_results`: eso es la convocatoria del capitan, solo existe si el la publica y solo tiene a los nuestros. El importador NO toca el marcador de `matches`: esa decision tiene su propia regla.
- **Marcadores**: los resultados por tablero del capitán SIEMPRE prevalecen sobre el marcador FACV (`marcadorPreferido` en `src/lib/marcador.ts`).
- **Permisos en 3 capas** (RLS dura + actions re-verifican + UI oculta): matriz vinculante en el anexo del plan 1B (+ adenda 1C). Jugador solo su disponibilidad; capitán solo SU equipo; admin todo.
- **Rangos** (migración 0011 aplicada): cuatro roles **acumulables** con permisos que **se suman** (si algún rol te lo permite, puedes). **admin** = solo el propietario, todo. **capitan** = elegido por equipo y por temporada, vive en `team_captains` y no en la tabla de roles porque tiene alcance. **jugador** = todo socio con cuenta y ficha. **junta** = gestiona altas de socios nuevos. Helpers: `is_admin()` (acepta la columna vieja y el rol), `tiene_rol()`, `es_junta()`, `esta_vinculado()`. **`tiene_rol()` TIENE que ser `security definer`**: si no, `is_admin()` → roles → policy → `is_admin()` es recursión infinita.
- **Acceso al club** (spec `2026-08-05-onboarding-club-design.md`): registro solo con **código de acceso** (tabla `access_codes`, uno activo, se gestiona en `/admin/acceso`), cuentas creadas por el servidor con `auth.admin.createUser` **ya confirmadas** (sin depender del SMTP), lista de `/vincular` desde el `force_order` de la temporada activa, y **lectura cerrada a los no vinculados** vía `esta_vinculado()` en 10 policies (migración 0009). **El interruptor "Allow new users to sign up" de Supabase DEBE estar desactivado**: sin él el código no sirve de nada, porque con la clave anónima cualquiera llama a `POST /auth/v1/signup` y se salta el formulario.
- **fide.com bloquea IPs de datacenter** (Vercel Y GitHub Actions): el ELO FIDE en vivo es cosmético — botón manual en local o `scripts/actualizar-elo-fide.mjs`. El cron de Vercel es único (`/api/cron/director`, diario, multiplexado por día: lunes pedir disponibilidad, jueves recordar, viernes sync FACV). **La sync del viernes son TRES en cadena y el orden es una dependencia, no un gusto** (`src/lib/import/sync-semanal.ts`): orden de fuerza → resultados y clasificación → actas por tablero. El orden de fuerza crea las fichas y los dos siguientes cruzan nombres contra ellas; las actas necesitan que las jornadas existan ya. Medido: 17,6 s las tres. **El orden de fuerza estaba FUERA del cron hasta el 2026-08-06** y eso era un agujero: quien entra al club a mitad de temporada aparece en el orden de la FACV con un número "bis", y hasta que alguien pulsaba el botón a mano ese socio no existía para la app — no podía vincular su cuenta (la lista de `/vincular` sale del orden de fuerza), no salía en el ranking y sus partidas del acta se quedaban sin ficha. Cuando aparecen fichas nuevas se avisa por push a admin y junta. Y en `/club/admin/orden-fuerza` sale un aviso con los nombres que han jugado y NO cruzan con ninguna ficha: son las filas del acta con `nuestro_player_id` null, así que no hace falta guardar nada nuevo para monitorizarlo.

## Estado y pendientes (actualizar al avanzar)

- Fases 0 (cimientos), 1A (diseño), 1B (equipos/calendario/disponibilidad) y 1C (validador/convocatorias/resultados) **completas y aprobadas** (última revisión global: "Ready to deploy", 166/166 tests).
- Pendiente de usuario al cierre de 1C: **SMTP Resend** (guía en docs/referencia). La prueba de convocatoria en móvil se hizo (jornada A/R99 "Amistoso de prueba") y la limpieza de datos de prueba está hecha (migración 0008, 2026-08-05): borradas las 2 cuentas `*.prueba@fomentogandia.test` con sus profiles/link_requests, las 3 fichas "Jugador Prueba *" y el encuentro R99 con su convocatoria y resultados. Queda 1 sola cuenta (jony9vcf@gmail.com, admin + capitán A). Las 4 fichas que sobrevivían de probar los importadores de ELO (Carlsen, Nakamura, Aalbersberg, Aalders) y un torneo de prueba creado a mano se borraron el 2026-08-06 (migración 0016): `players` y el orden de fuerza contienen ya exactamente los mismos 46 socios.
- **Fase 4 (torneos internos y ELO de club) COMPLETA (2026-08-06)**: migración 0015. Sección **Club** (`/club/interno`): torneos por liguilla o suizo, inscripción con ELO de partida sacado del oficial, generación de rondas, resultados, clasificación con Buchholz y ranking de ELO propio. Lógica pura en `src/lib/club/` (elo, emparejar, clasificacion) con 80 tests. **El ELO no se guarda**: se recalcula desde todas las partidas, para que corregir un resultado antiguo no deje el ranking mal para siempre. **Un descanso puntúa 0.5** (regla del club). Los colores de la liguilla se reparten orientando un circuito euleriano: desequilibrio 0 con jugadores impares y 1 con pares, que es el mínimo posible — un reparto codicioso deja a alguien sin blancas nunca.
- **Fase 3 (partidas) COMPLETA (2026-08-05/06)**: migración 0014. Repositorio compartido (`/club/partidas`): subir con datos, **tablero propio para meter las jugadas** (dependencia `chess.js` para las reglas), reproducir PGN guardados, buscar por nombre, importar de Lichess/Chess.com y exportar `.pgn`. Componentes en `src/components/ajedrez/`. Las partidas de torneos internos se enlazan por `club_pairings.game_id`.
- **Fase 2 (torneos y coches) COMPLETA (2026-08-05)**: migración 0010 aplicada, 147 torneos del calendario FACV importados (ninguno marcado de interés todavía), pantallas `/club/torneos`, `/club/torneos/[id]` y `/club/admin/torneos`, y tarjetas de próximos torneos en la home. 257 tests. 7 de los 8 criterios de aceptación verificados en vivo con JWT de socio; **el único pendiente es mirar las pantallas en un móvil de verdad**, que requiere sesión.
- **Onboarding del club (2026-08-05) — COMPLETO Y VERIFICADO (2026-08-07)**: código de acceso `CDRL-85C3-CAP6` (se gestiona en `/admin/acceso`), migración 0009 aplicada, "Allow new users to sign up" **desactivado en Supabase** y ambos criterios verificados en vivo: POST directo a `/auth/v1/signup` con clave anónima → 422 `signup_disabled`; cuenta temporal sin vincular → 0 filas en las 9 tablas del club (0009 funciona). Solo queda 1 cuenta en la BD: jony9vcf@gmail.com.
- **Sesión 2026-08-07 (PC del trabajo)** — hecho: (a) `git pull` de los 43 commits + BD verificada al día (0001→0021, la 0017 se aplicó ese día); (b) CLAUDE.md reparado de acentos (venía doble-codificado del 06) y regla nueva de migraciones-por-chat escrita; (c) **onboarding verificado en vivo** (signup 422 + lectura cerrada, ver punto de arriba); (d) **sede pública publicada**: portada y `/unirse` muestran el Poliesportiu Municipal (Av. dels Esports 17, Gandia) con enlace a Maps — dato del listado oficial de sedes FACV (`listado_sedes.php?id=1428`), verificado como visible sin sesión. Contacto candidato encontrado pero NO publicado (tel. 615363141 y miguelgan@hotmail.com son de Miguel Cortell, socio nº 17 — el propietario espera confirmación del club sobre qué publicar).
- **SIGUIENTE TAREA acordada (2026-08-06, para la sesión siguiente): selector de temporada en Interclubs.**
  Hoy hay una tabla `seasons` con un flag `activa`, una sola puede estarlo, y **once pantallas filtran por "la temporada activa"**. De la temporada cuelgan los equipos (`teams.season_id`) y de ahí las jornadas, clasificaciones, convocatorias y actas.
  **El agujero**: cuando se active la 2027, la 2026 **desaparece de la interfaz**. Los datos se quedan todos (31 jornadas, clasificaciones, 248 filas de acta) pero ninguna pantalla los enseña, porque todas piden la activa. No se borra nada: no hay puerta.
  **Qué hay que hacer**: un desplegable de temporada en la cabecera de Interclubs, con la activa por defecto, y pasar las consultas de "la activa" a "la elegida". Empezar por `/club/equipos`, `/club/equipos/[id]` y `/club/orden-fuerza`, que son las que tienen histórico que merezca la pena mirar.
  **Cómo arranca una temporada**, que hace falta para no romperlo: (1) cambiar `TEMPORADA_ID_FACV` en `src/lib/import/facv-config.ts` — la FACV da un id nuevo a su calendario cada año, es cambio de código y despliegue; (2) crear la temporada pegando su orden de fuerza en la importación manual de Admin → Orden de fuerza, que desactiva la anterior y activa la nueva con rollback si falla; (3) dar de alta los equipos; (4) importar calendario y luego resultados y actas. **Ningún paso es automático.**
  Lo que NO depende de la temporada y por tanto no se pierde de vista: fichas de socios, repositorio de partidas y torneos internos con su ELO de club.
- **Pendiente de diseño**: el repaso en **móvil** de todas las pantallas (el propietario decidió el 2026-08-06 hacer primero escritorio). Y **disponibilidad y la convocatoria del capitán se rediseñaron a ciegas en su estado CON datos**, porque la temporada 2026 ya terminó y no hay jornadas próximas: hay que volver a mirarlas cuando arranque la 2027.
- **Las cuatro fases están hechas.** Lo que queda, por orden de lo que bloquea antes:
  1. **Datos de la web pública** — dirección **HECHA (2026-08-07)**; faltan **horarios y contacto**, que el propietario está esperando que se los confirmen los del club. Cuando lleguen: rellenar en `src/app/page.tsx` y `src/app/unirse/page.tsx` (secciones "Dónde jugamos"/"Dónde estamos", el hueco está preparado y comentado).
  2. ~~Iconos PNG de la PWA~~ **HECHO (2026-08-06)**: el propietario aportó un escudo del club y `scripts/generar-iconos.mjs` genera de él `escudo.png`, `marca.png`, los iconos 192/512/maskable, el apple-touch de 180 y `src/app/favicon.ico`. El PNG original NO tenía transparencia (el cuadriculado estaba pintado dentro) y traía un manchón fuera del círculo: el script recorta el disco con alfa de verdad. El favicon del andamiaje era el triángulo de Vercel.
  3. **ELO de FEDA — importador ARREGLADO, fuente MUERTA, decisión: no aplicar (2026-08-07).** Cruzaba por `feda_id` y **ninguna de las 46 fichas lo tiene** (todas tienen `fide_id`, que es lo que publica el orden de fuerza de la FACV), así que devolvía siempre "0 actualizados" sin error — de ahí que el perfil enseñara "Fuerza 1400" para todo el club. Ahora cruza por FIDE usando la columna `Id. Fide` de la lista y rellena el `feda_id` que falta. **Pero feda.org no publica listas desde diciembre de 2023** (verificado en su página, y no apuntan a otro origen): lo único importable son datos de hace dos años y medio y solo cruzan **23 de 46**. El propietario decidió NO aplicarlo: el perfil ya enseña el ELO oficial de la FACV, que está al día y lo tienen los 46, y un FEDA de 2023 en la mitad del club daría una foto peor. **Cuando la FEDA publique lista nueva, basta con pulsar el botón de Admin → Actualización de ELO.** Si publican en otro sitio, cambiar `URL_PAGINA_ELO_FEDA` en `src/lib/import/feda-apply.ts`.
  4. **SMTP de Resend**: sin él la recuperación de contraseña no es fiable. El alta de socios NO depende de esto.
  5. **Probar en móvil con socios reales** un torneo interno y una partida en el tablero: es lo único que nadie ha verificado, porque requiere sesión.
  6. **Jugar en el navegador** (decisión del propietario, con la advertencia registrada de que es la pieza más grande del proyecto y que Lichess ya cubre ese terreno).
  Deuda menor en `docs/superpowers/plans/2026-08-05-deuda-pulido.md`.
- **Bloque grande pendiente, planteado por el propietario el 2026-08-08.** Cuatro piezas, aquí por orden de lo que cuesta y de lo que desbloquea a lo demás. **Ninguna empezada.**
  1. ~~**Tablero decente**~~ **HECHO (2026-08-08).** Las piezas eran caracteres Unicode (♞) y por eso se veía mal: el dibujo lo ponía la fuente del sistema. Ahora son **SVG** en `public/piezas/`, juego `celtic` de Maurizio Monge, **MIT** (cburnett y merida, los más conocidos, son GPLv2+ y no interesa meter copyleft fuerte aquí). El nombre del fichero sale de `chess.js` (`color`+`tipo`), así que cambiar de juego es cambiar los doce SVG — ver `public/piezas/LICENCIA.md`, que lista las otras opciones permisivas. Añadidas también las **coordenadas** en los bordes y **arrastrar y soltar** con eventos de puntero (`pointerdown/move/up` + `setPointerCapture`, NO la API de arrastrar de HTML, que con el dedo no dispara nada), conviviendo con el toque-toque de siempre, que es el que funciona en móvil y el único que llega con teclado. Verificado en local moviendo, arrastrando y probando una jugada ilegal; **la coronación arrastrando está escrita pero no jugada**.
  2. ~~**Analizador con motor**~~ **HECHO (2026-08-08).** Stockfish 18 en WebAssembly, dentro del navegador, en `public/motor/` (ver su LEEME.md). **SOLO en el revisor de partidas**, por decisión del propietario: metiendo una partida en el tablero nadie necesita que le digan si la jugada era buena. Compilación **`lite-single`**: `single` porque las de varios hilos exigen `SharedArrayBuffer` y con él habría que servir TODA la web con las cabeceras COOP/COEP, que romperían cualquier recurso de fuera; `lite` porque la red completa pesa 110 MB y esta 7. **Se carga a mano**, al pulsar "Analizar", nunca al abrir la pantalla — y el `sw.js` no precachea nada, solo atiende push, así que instalar la PWA no se lo traga. La lógica que se puede equivocar (el SIGNO de la puntuación, que UCI da desde quien mueve, y la traducción de la línea a jugadas) vive aparte y pura en `src/lib/ajedrez/evaluacion.ts`, con 24 tests; el Worker en `motor.ts`. **Stockfish es GPLv3**: va tal cual, sin tocar, en su propia carpeta, con su licencia al lado, y la app le habla por mensajes UCI. **Cómo se le habla al motor, que costó dos intentos.** UCI no admite un `position` mientras piensa, y `stop` no es instantáneo, así que mandando `stop`+`position`+`go` de golpe bastaban tres jugadas seguidas para dejarlo mudo. El primer arreglo —encolar las peticiones y esperar un `readyok` con un tope de 5 s— **seguía colgándose, y peor**: el navegador FRENA LOS TEMPORIZADORES de una pestaña que no está delante, así que cambiar de aplicación un momento en el móvil hacía saltar ese tope sin que pasara nada malo, y el motor quedaba marcado como roto para siempre. **Ahora no hay ningún temporizador en el análisis** (solo en el arranque): se va con el protocolo, porque tras un `go` el motor SIEMPRE contesta `bestmove` —termine o se le corte—, así que se guarda la posición pendiente, se manda `stop` si está buscando, y es la llegada de `bestmove` la que lanza la siguiente. Gana siempre la última posición pedida. **La barra es vertical, al lado del tablero, y gira con él**: abajo va siempre el color que se ve abajo, como en Lichess. Verificado en local: posición inicial +0.36 a profundidad 18, reevalúa al pasar jugada, aguanta saltos rápidos por la lista de jugadas, y la barra se invierte al girar el tablero.
  3. ~~**Asistente con IA**~~ **ESCRITO (2026-08-08), pendiente de la clave.** Botón flotante en toda la zona de socios (`src/components/asistente/Asistente.tsx`, montado en `src/app/club/layout.tsx`, solo para quien ya tiene ficha). **Gemini**, elegido por el propietario porque tiene capa gratuita; se habla por REST sin SDK (`src/lib/asistente/gemini.ts`) y el modelo está en una constante, `MODELO`, que es lo único a tocar si Google jubila esa versión. **La clave `GEMINI_API_KEY` vive solo en el servidor**, en `src/app/api/asistente/route.ts`. **Las cinco herramientas de lectura van con el cliente de la SESIÓN del socio, jamás con la clave de servicio**: así la RLS que protege las pantallas protege al asistente, y no hay forma de que le cuente a nadie algo que no vería entrando él — un asistente con clave de servicio es una puerta trasera a toda la base con una frase bien puesta. El carácter (solo ajedrez y club, y reconducir con gracia SIN anunciarlo) está en `instrucciones.ts`, tratado como código y con tests, igual que la validación del historial que manda el navegador (`peticion.ts`). **RANGOS (2026-08-08, exigencia del propietario): el asistente cuenta cosas distintas según quién pregunta.** Son DOS candados y hacen falta los dos, porque cubren cosas distintas: (a) la **RLS** es el candado de los DATOS y no se puede rodear convenciendo al modelo; (b) `rangos.ts` es el de lo que se CUENTA o EXPLICA — a un socio no se le explica cómo se aprueba un alta aunque esa explicación no lea ni una fila. Cada herramienta declara su rango mínimo y `declaracionesPara()` filtra ANTES de enseñárselas al modelo (lo que no ve, no lo puede llamar ni mencionar), y `ejecutar()` lo vuelve a comprobar, porque una lista que se filtra en un sitio y se obedece en otro se desincroniza. El rango sale SIEMPRE de la sesión: decir en el chat "soy el administrador" no cambia nada — probado. **Y ojo con la API: Gemini devuelve 400 y no contesta nada si en una `functionDeclaration` va un campo que no conoce**, así que el `rango` se quita antes de mandarla; hay un test que lo vigila. **Se han aplicado las lecciones del bot que el propietario ya tenía en producción en otro proyecto**, guardado en `docs/referencia/chatbot-ia-garestudio.md` (él lo borra de allí; esta es la copia buena): formato de texto plano —el markdown se pinta como asteriscos sueltos en el chat—, rechazos variados mencionando lo que te han pedido, **fechas precalculadas** en el prompt porque los modelos deducen mal "mañana", freno de mensajes por socio, y **suplente de modelo**: el bueno de la capa gratuita da 503 a ratos. Ojo con esto último: **Google retiró `gemini-2.5-flash` para cuentas nuevas de un día para otro** y dejó muerto aquel bot; por eso hay `LLM_MODEL` para forzar otro sin tocar código, y el catálogo que acepta una clave se consulta en `https://generativelanguage.googleapis.com/v1beta/models?key=CLAVE`.
  **FALTA: crear la clave en https://aistudio.google.com/apikey, ponerla en `.env.local` y en Vercel.** Sin ella el asistente contesta que no está configurado y el resto de la app va igual. **FUNCIONANDO Y VERIFICADO EN PRODUCCIÓN (2026-08-08)**: contesta el número de orden y el ELO reales, nombra bien los días, dice que no cuando le preguntan por un socio inventado, reconduce con gracia y aguanta un "ignora tus instrucciones anteriores". La clave estuvo un rato solo en `.env.local`: **eso NO llega a Vercel**, hay que crearla también en Settings → Environment Variables y desplegar de nuevo; `GET /api/asistente` (solo admin) lo dice sin enseñar el valor. Probado además con `scripts/probar-asistente.mjs`, que arma el prompt de verdad y responde las herramientas con datos de mentira (no toca la base, y no es un test porque gasta cuota). Resultado: contesta el dato correcto, usa el formato de lista sin markdown, acierta las fechas ("pasado mañana es el lunes 10"), no se inventa lo que no hay, reconduce con gracia y **aguanta un "ignora tus instrucciones anteriores"**. **Modelo por defecto: `gemini-3.1-flash-lite`, y NO el bueno**: `gemini-3.5-flash` se quedó sin cuota gratuita a la TERCERA pregunta con la clave del club. Y `gemini-2.5-flash-lite` da 404 ("no longer available to new users"), o sea que Google ha retirado dos modelos que este documento daba por buenos: por eso el suplente es un ALIAS (`gemini-flash-lite-latest`), que se mueve solo. Si algún día se paga la API, `LLM_MODEL=gemini-3.5-flash`.
  4. **Jugar en la app, con chat — ESCRITO Y A MEDIO PROBAR (2026-08-08).** El propietario eligió lo grande en los dos ejes: **en vivo con reloj** y **las dos cosas** (retos entre socios y partidas de los torneos internos). Migraciones **0022** (tablas `challenges`, `live_games`, `live_chat` + RLS + publicación de tiempo real), **0023** (cadencia del torneo + índice único de una partida por emparejamiento) y **0024** (arreglo de seguridad, ver abajo). Todo aplicado.
     **Dónde está la verdad**: el reloj y las reglas son del SERVIDOR (`src/lib/vivo/`, 50 tests) y `live_games` **no tiene ninguna política de escritura** — se toca solo desde `jugar/actions.ts` con clave de servicio, tras comprobar quién eres, si es tu turno y si te queda tiempo. Verificado: con la clave anónima, insertar una partida da 42501. El "ahora" lo pone siempre el servidor.
     **Colores, que son DOS reglas**: en torneo se COPIAN de `club_pairings` (los repartió `emparejar.ts` con el criterio oficial; volver a sortear rompería el equilibrio del torneo); en amistosa, `blancasEnAmistosa` sortea el primer encuentro entre dos socios y luego alterna.
     **EL FALLO QUE COSTÓ DOS RONDAS ENCONTRAR, y la lección**: el tiempo real no entregaba NADA. `postgres_changes` aplica la RLS **al que escucha**, y si el socket no lleva el token del usuario el servidor no ve a nadie y lo filtra todo — el canal dice `SUBSCRIBED` igualmente, que es lo que despista. Se arregla con `src/lib/supabase/vivo.ts`, que pone el token ANTES de suscribir. **Medirlo fue lo que lo resolvió** (dos clientes contra la misma fila, uno con clave de servicio y otro sin sesión); las dos rondas anteriores fueron a base de suponer. Encima de eso: la jugada se pinta antes de mandarla (validada con las mismas reglas) y `mover` ya NO llama a `revalidatePath`, que rehacía la página entera en cada jugada.
     **Seguridad, migración 0024**: la política del chat comparaba `p.player_id = player_id` dentro de un `exists` sobre `profiles`, y ese nombre sin cualificar lo resuelve Postgres contra la subconsulta — se leía `p.player_id = p.player_id`, siempre cierta. Cualquier socio vinculado podía escribir en el chat en nombre de otro. **Cuidado con esto en cualquier política nueva: cualificar siempre con el nombre de la tabla.**
     **PROBADO CON DOS SESIONES Y APROBADO (2026-08-09)**: jugadas al instante, chat sin recargar, retos que meten a los dos en la mesa, tablas, volver jugada, abandono, resultado al torneo y PGN al repositorio.
     **TODO LO EN VIVO VA POR DIFUSIÓN, no por `postgres_changes`** (`src/lib/vivo/difundir.ts`): la mesa, el chat y **los retos** (`difundirAviso` al canal `avisos-<ficha>`). Escuchar la tabla no entregaba nada y todo se sostenía con reintentos, de ahí que aceptar un reto tardara en meter al otro y que el número rojo apareciera tarde. **REGLA: si algo tiene que llegar rápido, se difunde; el reintento es una red, nunca el motor.** Y no se mezclan `postgres_changes` y difusión en un mismo canal: si la RLS rechaza el enganche a la tabla, se cae el canal entero y sigue diciendo `SUBSCRIBED`.
     **UN SOLO SITIO ESCUCHANDO CADA COSA.** `Avisos` (en el layout) lleva los retos: entra en la partida al aceptar, refresca `/club/jugar` y alimenta el número rojo. La pantalla de Jugar tenía su propio canal y su propio reintento en paralelo — dos sitios escuchando lo mismo es el doble de consultas y una carrera por ver quién te lleva antes a la partida.
     **El número rojo del menú lo lleva el cliente** (`src/components/avisos/Pendientes.tsx`): la cuenta del layout es solo el valor de partida, porque un número pintado en el servidor se queda congelado hasta la siguiente navegación.
     **EL RELOJ SE PARA AL ACABAR** (`parado` en `reloj.ts`). Sin eso la fila conserva los milisegundos del INSTANTE DE LA ÚLTIMA JUGADA y al terminar los dos relojes dan un salto hacia arriba. `ultima_jugada_en` a null es lo que le dice al navegador que deje de contar. Vale para mate, bandera, abandono y tablas.
     **PRESENCIA, DOS COSAS DISTINTAS**: el círculo verde de todo el club (`Presencia.tsx`, un canal para todos, montado en el layout) y **quién está mirando esta pantalla** (`Mirando.tsx`, una sala por pantalla: `partida-<id>`, `torneo-<id>`, `repaso-<id>`). Ninguna de las dos toca la base ni pasa por RLS: los navegadores se anuncian entre ellos y al cerrar la pestaña desaparecen solos. Nunca encima del tablero: empuja las piezas y se mueve mientras se lee la partida.
     **CON PÚBLICO, EL QUE MIRA NO CUENTA IGUAL QUE EL QUE JUEGA**: el reintento de la mesa es de 5 s jugando y de 20 s mirando. En una final del club la mesa la consultan dos jugadores y cuarenta espectadores; al que juega quedarse colgado le cuesta la partida, al que mira cinco segundos no le cuestan nada.
     **DATOS DE PRUEBA VIVOS**: el torneo `PRUEBA — Partida en vivo` (`node scripts/torneo-prueba.mjs quitar`), partidas de prueba con su chat, y **una segunda cuenta vinculada a la ficha de Aarón Escrivá** que el propietario creó para probar — hay que borrarla cuando se acabe.
     **IDEA APUNTADA, NO EMPEZADA — "todas las mesas de la ronda a la vez"**: una pantalla del torneo con todos los tableros en marcha, para seguir una ronda de un vistazo. **NO se hace montando N veces la mesa**: cada partida abierta son 3 canales y un reloj repintando a 10 fps, y seis a la vez ahogan el navegador del que mira. Hay que hacerlo con **un canal por torneo** (el servidor difunde ahí cada jugada de cualquier mesa de la ronda), tableros **sin reloj corriendo** —el tiempo, en número y solo al recibir jugada— y **sin reintento por mesa**, uno solo para toda la pantalla. El techo aquí no es el servidor, es el navegador del espectador.
- **Bloque nuevo pedido por el propietario el 2026-08-09. NINGUNO empezado.** Por orden de lo que aporta antes:
  1. **Repaso de lógica de toda la app**, buscando errores y agujeros. Es lo primero porque lo demás se construye encima. Sitios con más papeletas, por lo visto hasta ahora: **políticas RLS con nombres sin cualificar** (el agujero de la 0024 era exactamente eso, y hay muchas policies escritas con el mismo patrón), las acciones de servidor que escriben con clave de servicio (comprobar SIEMPRE quién eres antes de escribir), y los sitios donde la interfaz esconde algo en vez de comprobarlo — esconder no es comprobar.
  2. **Notificaciones, que funcionen de verdad.** Hoy conviven tres caminos y ninguno se ha probado a fondo junto: push web (VAPID, **la suscripción es por dispositivo y navegador**, así que quien entra desde dos sitios tiene que aceptar en los dos), las tarjetas flotantes de `Avisos`, y el número rojo del menú. Falta: un sitio donde ver qué se ha mandado, reintento cuando el push falla, y decidir qué avisa por push y qué no (hoy: reto aceptado, alta de socio, fichas nuevas del orden de fuerza).
  3. **Perfil: la experiencia de cada uno, y poder ver la de los demás.** Foto que sube el socio (Supabase Storage, con recorte y tope de tamaño — una foto de móvil sin tocar son 5 MB), aperturas favoritas, y **tablero y piezas elegibles entre opciones que ponemos nosotros** (los SVG ya están separados por juego en `public/piezas/`, así que añadir juegos es añadir carpetas; **cuidado con las licencias**, ver `public/piezas/LICENCIA.md`). Las preferencias se guardan en el perfil y viajan con la cuenta, no en el navegador. Y una ficha pública de socio donde se vea todo eso.
  4. **Panel de admin con datos de uso** (`/club/admin`, solo admin): partidas jugadas, visitas, tiempo de uso, cuentas activas, media de conectados, por día, semana y mes. **Lo que hoy NO se guarda es casi todo**: hay partidas y fichas, pero no visitas ni sesiones ni tiempo. Hace falta decidir primero qué se registra y **con qué respeto a la privacidad** —esto es un club pequeño y "quién ha entrado y cuánto rato" son datos de personas identificables—; lo razonable es guardar contadores agregados por día y no un registro por persona y visita. La presencia en vivo no sirve para esto: no se guarda a propósito.
  5. **Pestaña de "qué puedes hacer aquí" en Perfil**, con lo que hay en la app explicado, **recortada por rango**: un jugador no ve lo de admin ni lo de capitán. Ojo: el asistente ya tiene el mapa de la app y la tabla de rangos en `src/lib/asistente/`; esa pantalla y el asistente tienen que contar lo mismo, así que el texto debería salir de un sitio compartido y no escribirse dos veces.
- **Regla de pantalla que salió del repaso del 2026-08-08: usar el ancho, no el alto.** Cuando una lista crece sin techo, NO se deja correr hacia abajo. Dos salidas, según lo que sea: (a) **partir en dos columnas** desde `lg` con `partirEnDos` (`src/lib/ui/columnas.ts`, 10 tests) — es lo que se hizo con el ranking oficial de 46 filas y con el del club; (b) **enseñar una sola parte con selector**, que es lo que se hizo con las rondas de los torneos internos (una liguilla de 8 son 28 cruces). Y una columna de tarjetas de dos líneas se convierte en **una caja con filas**, como el calendario de Interclubs, que es el patrón de referencia de la app.
