@AGENTS.md

# Chess Club Manager â€” Fomento de Gandia Â· GuÃ­a de trabajo

App PWA del club de ajedrez Fomento de Gandia (GandÃ­a). Propietario: J. Ribes ("Joan", admin del club, capitÃ¡n del equipo A, ficha "Joan MartÃ­nez Ribes"). Idioma de trabajo: **espaÃ±ol**.

## DÃ³nde estÃ¡ todo

- **Estructura de rutas (2026-08-05, retocada el 2026-08-06):** `/` es la **web pÃºblica** del club; la app de socios vive bajo **`/club`**. Cinco secciones en la navegaciÃ³n: Inicio, Interclubs (`/club/equipos`, `/club/disponibilidad`, `/club/jornadas`), **Torneos** (`/club/torneos`, con dos pestaÃ±as: `/club/torneos/facv` los de fuera y `/club/torneos/interno` los del club), Partidas y Perfil, mÃ¡s Admin. **Torneos y Club se fusionaron el 2026-08-06** por decisiÃ³n del propietario: para el socio los dos son torneos, y siete pestaÃ±as no caben en un mÃ³vil. NO se mezclan en una lista con filtro porque lo que haces en cada uno no se parece (en los de fuera dices si vas y se organizan coches; en los del club te inscribes y el resultado cuenta para el ELO interno). `/club/torneos` **redirige** a la pestaÃ±a de fuera y existe solo para no romper las notificaciones push ya enviadas, que apuntan ahÃ­. `/login`, `/registro`, `/auth/confirm` y `/api` se quedan en la raÃ­z. El gate de "sin ficha aprobada" es el layout del grupo `/club/(vinculado)`; `vincular` y `perfil` cuelgan de `/club` fuera del grupo. Rumbo completo en `docs/superpowers/specs/2026-08-05-arquitectura-objetivo.md`.
- **ProducciÃ³n:** https://fomento-gandia-chess-swart.vercel.app (Vercel despliega `main` automÃ¡ticamente)
- **Repo:** https://github.com/AndraKinG/fomento-gandia-chess (cuenta GitHub principal del usuario)
- **Supabase:** proyecto `fomento-gandia-chess` en su cuenta Google secundaria (jony9vcf@...). Cuenta real del usuario en la app: jony9vcf@gmail.com (admin + capitÃ¡n A)
- **Specs y planes:** `docs/superpowers/specs/` y `docs/superpowers/plans/` (Fases 0, 1A, 1B, 1C completadas)
- **Referencia de dominio:** `docs/referencia/` â€” reglamento RGC FACV extraÃ­do, verificaciÃ³n empÃ­rica de ambigÃ¼edades, guÃ­a SMTP Resend
- **`.env.local` NO estÃ¡ en el repo** (gitignorado): claves de Supabase (nuevas `sb_publishable_`/`sb_secret_`), VAPID pÃºblica/privada (deben ser LAS MISMAS en todas las mÃ¡quinas â€” las suscripciones push dependen de ellas), `CRON_SECRET`. Al montar el proyecto en otra mÃ¡quina, copiar el archivo por canal privado; el resto estÃ¡ en el README.

## CÃ³mo trabajamos (reglas del propietario)

1. **Claude NUNCA hace `git push`.** Se commitea en local y se avisa al usuario con un bloque de comandos copy-paste que SIEMPRE empieza por `cd "<ruta del proyecto>"`. Ã‰l pushea con su cuenta.
2. **Migraciones SQL**: Claude escribe el fichero en `supabase/migrations/` (numeradas, en orden) y se lo deja al usuario en el portapapeles (`Set-Clipboard`) para que lo pegue en el SQL Editor de Supabase. Verificar despuÃ©s vÃ­a REST que las tablas/columnas existen. Aplicadas hasta ahora: 0001â†’0016 (la 0017 borra el torneo de prueba "test"). **Al pasar SQL al portapapeles usar `Get-Content -Raw -Encoding UTF8`**: PowerShell 5.1 lee los ficheros sin BOM como ANSI y corrompe los acentos (`Ã³` â†’ `ÃƒÂ³`), que en un literal de texto acaba guardado mal en la BD.
3. **Flujo de desarrollo**: brainstorm â†’ spec escrita y aprobada â†’ plan de tareas pequeÃ±as â†’ ejecuciÃ³n por subagentes con revisiÃ³n (spec + calidad) por tarea, fix-loops hasta aprobar, y revisiÃ³n global adversarial al final de cada fase (plugin `superpowers` de Claude Code; el ledger local vive en `.superpowers/sdd/progress.md`, gitignorado).
4. **Prioridad lÃ³gica sobre pulido** (decisiÃ³n expresa del usuario): hallazgos visuales menores van al ledger para una pasada de pulido global futura, NO a fix-loops â€” salvo roturas de layout o accesibilidad grave. La lÃ³gica (validador, permisos, datos) mantiene el listÃ³n completo.
5. **Gates de usuario**: acciones que requieren sus cuentas (SQL Editor, Vercel, Resend, push de git) se le piden con pasos exactos clic a clic. Es desarrollador junior: instrucciones concretas, sin jerga innecesaria.
6. **VerificaciÃ³n en vivo con datos reales** siempre que sea posible (la BD compartida tiene el orden de fuerza real de 46 jugadores, calendario y resultados 2026 reales). No borrar/alterar datos reales sin permiso; los datos de prueba se etiquetan claramente (p. ej. rival "PRUEBA - BORRAR").

## Decisiones tÃ©cnicas clave (no cambiar sin preguntar)

- **Identidad visual**: blanco y azul "gandiblues". Doble tema: claro *MediterrÃ¡neo* / oscuro *Azul profundo* (tokens en `globals.css`, en espaÃ±ol: `bg-fondo`, `text-tinta`, `text-acento-texto`...). Tablero de ajedrez blanquiazul cuando llegue la Fase 3. **`--tinta-suave` del tema claro es `#556577` por accesibilidad, no por gusto**: el `#64748b` original daba 4.46 de contraste sobre `--fondo` y AA pide 4.50. No aclararlo. Los 18 pares medidos de ambos temas pasan AA (ver `docs/superpowers/plans/2026-08-05-deuda-pulido.md`).
- **Ancho y navegacion (2026-08-06)**: el ancho de pantalla NO se escribe a mano. Se usa `Contenedor` (`src/components/ui/Contenedor.tsx`) con tres medidas segun para que es el contenido: `formulario` (una columna, ensanchar un campo de texto lo empeora), `lectura` (detalle y texto seguido) y `panel` (listas, tablas y rejillas, es el que aprovecha el monitor). **La `Cabecera` tiene que llevar la MISMA medida que el `Contenedor` de su pantalla** o el titulo queda desalineado respecto a las tarjetas. Las listas de tarjetas usan `REJILLA[2]`/`REJILLA[3]` (o el componente `Rejilla`) para pasar de 1 a 2 o 3 columnas. La navegacion es una sola lista en dos formas (`src/components/Navegacion.tsx`): `NavLateral` desde `lg` (1024 px) y `NavInferior` por debajo, montadas en `src/app/club/layout.tsx`. En escritorio la barra abajo es la peor opcion porque el hueco que sobra es horizontal.
- **Velocidad percibida**: cada seccion de `/club` tiene su `loading.tsx` con `Cargando` (esqueleto con la forma de lo que viene). No es cosmetico: sin `loading.tsx` Next **no precarga nada** de una ruta dinamica, y todas las de `/club` lo son. Las acciones de servidor en `<form action>` van con `BotonAccion` (`useFormStatus`), que se deshabilita y cambia de texto; los Client Components ya lo hacen con `useTransition`. Y las consultas de una pantalla van en tandas con `Promise.all`, no en fila: la home pasaba de diez viajes seguidos a Supabase a dos tandas, y usa `sesionActual()` (memoizada con `cache()`, el layout ya la ha llamado) en vez de repetir `getUser` + `profiles`.
- **Fuerza del jugador** = `force_order.elo_oficial` (orden de fuerza oficial FACV, sincronizado de `of_publico.php?id=56`); fallback `max(FEDA, FIDE)` (RGC art. 52.1). IDs de club/temporada FACV en `src/lib/import/facv-config.ts` (actualizar cada temporada).
- **Validador RGC** (`src/lib/validador/`): mÃ³dulo puro, flag REQUERIDO `permitirInversionDentroMargen` â€” **estricto (false) por defecto**; dos ambigÃ¼edades del reglamento documentadas en `docs/referencia/verificacion-empirica-rgc.md` (0 inversiones en 11 rondas reales de 2026). No relajar sin confirmaciÃ³n FACV del usuario.
- **Publicar convocatoria**: Ãºnica puerta = server action con re-validaciÃ³n completa + escritura service_role (trigger de blindaje en migraciÃ³n 0007). El cliente valida en vivo solo como ayuda.
- **Marcadores**: los resultados por tablero del capitÃ¡n SIEMPRE prevalecen sobre el marcador FACV (`marcadorPreferido` en `src/lib/marcador.ts`).
- **Permisos en 3 capas** (RLS dura + actions re-verifican + UI oculta): matriz vinculante en el anexo del plan 1B (+ adenda 1C). Jugador solo su disponibilidad; capitÃ¡n solo SU equipo; admin todo.
- **Rangos** (migraciÃ³n 0011 aplicada): cuatro roles **acumulables** con permisos que **se suman** (si algÃºn rol te lo permite, puedes). **admin** = solo el propietario, todo. **capitan** = elegido por equipo y por temporada, vive en `team_captains` y no en la tabla de roles porque tiene alcance. **jugador** = todo socio con cuenta y ficha. **junta** = gestiona altas de socios nuevos. Helpers: `is_admin()` (acepta la columna vieja y el rol), `tiene_rol()`, `es_junta()`, `esta_vinculado()`. **`tiene_rol()` TIENE que ser `security definer`**: si no, `is_admin()` â†’ roles â†’ policy â†’ `is_admin()` es recursiÃ³n infinita.
- **Acceso al club** (spec `2026-08-05-onboarding-club-design.md`): registro solo con **cÃ³digo de acceso** (tabla `access_codes`, uno activo, se gestiona en `/admin/acceso`), cuentas creadas por el servidor con `auth.admin.createUser` **ya confirmadas** (sin depender del SMTP), lista de `/vincular` desde el `force_order` de la temporada activa, y **lectura cerrada a los no vinculados** vÃ­a `esta_vinculado()` en 10 policies (migraciÃ³n 0009). **El interruptor "Allow new users to sign up" de Supabase DEBE estar desactivado**: sin Ã©l el cÃ³digo no sirve de nada, porque con la clave anÃ³nima cualquiera llama a `POST /auth/v1/signup` y se salta el formulario.
- **fide.com bloquea IPs de datacenter** (Vercel Y GitHub Actions): el ELO FIDE en vivo es cosmÃ©tico â€” botÃ³n manual en local o `scripts/actualizar-elo-fide.mjs`. El cron de Vercel es Ãºnico (`/api/cron/director`, diario, multiplexado por dÃ­a: lunes pedir disponibilidad, jueves recordar, viernes sync FACV).

## Estado y pendientes (actualizar al avanzar)

- Fases 0 (cimientos), 1A (diseÃ±o), 1B (equipos/calendario/disponibilidad) y 1C (validador/convocatorias/resultados) **completas y aprobadas** (Ãºltima revisiÃ³n global: "Ready to deploy", 166/166 tests).
- Pendiente de usuario al cierre de 1C: **SMTP Resend** (guÃ­a en docs/referencia). La prueba de convocatoria en mÃ³vil se hizo (jornada A/R99 "Amistoso de prueba") y la limpieza de datos de prueba estÃ¡ hecha (migraciÃ³n 0008, 2026-08-05): borradas las 2 cuentas `*.prueba@fomentogandia.test` con sus profiles/link_requests, las 3 fichas "Jugador Prueba *" y el encuentro R99 con su convocatoria y resultados. Queda 1 sola cuenta (jony9vcf@gmail.com, admin + capitÃ¡n A). Las 4 fichas que sobrevivÃ­an de probar los importadores de ELO (Carlsen, Nakamura, Aalbersberg, Aalders) y un torneo de prueba creado a mano se borraron el 2026-08-06 (migraciÃ³n 0016): `players` y el orden de fuerza contienen ya exactamente los mismos 46 socios.
- **Fase 4 (torneos internos y ELO de club) COMPLETA (2026-08-06)**: migraciÃ³n 0015. SecciÃ³n **Club** (`/club/interno`): torneos por liguilla o suizo, inscripciÃ³n con ELO de partida sacado del oficial, generaciÃ³n de rondas, resultados, clasificaciÃ³n con Buchholz y ranking de ELO propio. LÃ³gica pura en `src/lib/club/` (elo, emparejar, clasificacion) con 80 tests. **El ELO no se guarda**: se recalcula desde todas las partidas, para que corregir un resultado antiguo no deje el ranking mal para siempre. **Un descanso puntÃºa 0.5** (regla del club). Los colores de la liguilla se reparten orientando un circuito euleriano: desequilibrio 0 con jugadores impares y 1 con pares, que es el mÃ­nimo posible â€” un reparto codicioso deja a alguien sin blancas nunca.
- **Fase 3 (partidas) COMPLETA (2026-08-05/06)**: migraciÃ³n 0014. Repositorio compartido (`/club/partidas`): subir con datos, **tablero propio para meter las jugadas** (dependencia `chess.js` para las reglas), reproducir PGN guardados, buscar por nombre, importar de Lichess/Chess.com y exportar `.pgn`. Componentes en `src/components/ajedrez/`. Las partidas de torneos internos se enlazan por `club_pairings.game_id`.
- **Fase 2 (torneos y coches) COMPLETA (2026-08-05)**: migraciÃ³n 0010 aplicada, 147 torneos del calendario FACV importados (ninguno marcado de interÃ©s todavÃ­a), pantallas `/club/torneos`, `/club/torneos/[id]` y `/club/admin/torneos`, y tarjetas de prÃ³ximos torneos en la home. 257 tests. 7 de los 8 criterios de aceptaciÃ³n verificados en vivo con JWT de socio; **el Ãºnico pendiente es mirar las pantallas en un mÃ³vil de verdad**, que requiere sesiÃ³n.
- **Onboarding del club (2026-08-05)**: spec y plan escritos y aprobados, cÃ³digo implementado (180 tests). MigraciÃ³n 0009 **aplicada**. CÃ³digo de acceso inicial: `CDRL-85C3-CAP6`. **Pendiente del usuario**: (a) desplegar el cÃ³digo a Vercel, (b) desactivar "Allow new users to sign up" en Supabase, (c) verificar despuÃ©s el criterio de aceptaciÃ³n 1 de la spec (que un POST directo a `/auth/v1/signup` con la clave anÃ³nima falle) y el 3 (que una cuenta sin aprobar no lea nada, hace falta una cuenta temporal de prueba).
- **Las cuatro fases estÃ¡n hechas.** Lo que queda, por orden de lo que bloquea antes:
  1. **Datos de la web pÃºblica** (direcciÃ³n, horarios, contacto, escudo): solo los puede poner el propietario, y sin ellos `/unirse` queda desnudo. Marcado como pendiente en `src/app/page.tsx`.
  2. ~~Iconos PNG de la PWA~~ **HECHO (2026-08-06)**: el propietario aportÃ³ un escudo del club y `scripts/generar-iconos.mjs` genera de Ã©l `escudo.png`, `marca.png`, los iconos 192/512/maskable, el apple-touch de 180 y `src/app/favicon.ico`. El PNG original NO tenÃ­a transparencia (el cuadriculado estaba pintado dentro) y traÃ­a un manchÃ³n fuera del cÃ­rculo: el script recorta el disco con alfa de verdad. El favicon del andamiaje era el triÃ¡ngulo de Vercel.
  3. **SMTP de Resend**: sin Ã©l la recuperaciÃ³n de contraseÃ±a no es fiable. El alta de socios NO depende de esto.
  4. **Probar en mÃ³vil con socios reales** un torneo interno y una partida en el tablero: es lo Ãºnico que nadie ha verificado, porque requiere sesiÃ³n.
  5. **Jugar en el navegador** (decisiÃ³n del propietario, con la advertencia registrada de que es la pieza mÃ¡s grande del proyecto y que Lichess ya cubre ese terreno).
  Deuda menor en `docs/superpowers/plans/2026-08-05-deuda-pulido.md`.
